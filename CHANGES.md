# 변경 사항 정리

---

## v1.6.256 — 관리자 정산 이력 클럽별 그룹 보기

- **전체 보기 구조 개선**: 관리자 클럽별 정산 이력의 기본 보기를 월별 혼합 목록에서 클럽별 그룹으로 변경
- **자동 분류**: 새 정산 이력은 클럽명 기준으로 해당 그룹의 최신 이력 위치에 자동 표시
- **빠른 요약**: 각 클럽 그룹의 `사용 요약` 버튼으로 행사별 비용·지원금·자부담 표를 바로 확인
- **기존 관리 기능 유지**: 그룹 안의 개별 이력에서 수정·삭제·영수증 확인은 그대로 가능

---

## v1.6.255 — 사용자 정산 이력 카테고리 표시 통일

- **상세 내역 정보 일치**: 일반 사용자 정산 이력에도 각 비용 항목의 `행사비`·`시설 및 장비 이용료`·`상품비`를 관리자 화면과 동일하게 표시
- **구버전 이력 보호**: 카테고리 정보가 없는 과거 항목은 `미분류`로 표시해 빈 값이나 오류가 보이지 않도록 처리

---

## v1.6.254 — 월별 차트 한국 원화 표기

- **세로축**: `K`·`M` 대신 `50만`, `100만`, `450만`처럼 한국 원화 단위로 표시
- **막대 합계**: `95K`, `3.8M` 대신 `9만 5천원`, `380만원`처럼 원 단위까지 표시
- **축 제목**: 세로축에 `금액 (원)`을 명시

---

## v1.6.253 — 클럽 사용 요약에 예산 현황 추가

- **클럽 총 예산·현재 잔여 예산**: 클럽별 정산 횟수 차트에서 여는 사용 요약 팝업 상단에 추가
- **일관된 산정 기준**: 관리자 클럽 레지스트리의 배정 예산과 `기존 사용액 + 올해 확정 회사 지원금`을 사용해 정산 화면의 잔여 예산과 동일한 기준으로 표시
- **예산 초과 표시**: 잔여 예산이 음수일 경우 경고색으로 표시

---

## v1.6.252 — 지원금·자부담 요약 및 클럽 사용 내역

- 자부담 / 회사지원금 추이 카드에 현재 차트 범위의 총 회사 지원금과 총 자부담 비용 요약 추가
- 클럽별 정산 횟수 차트의 막대를 클릭하면 해당 클럽의 올해 정산 내역을 팝업 표로 표시
- 행사 날짜, 참석 인원, 행사비, 시설·장비 이용료, 상품비, 회사 지원금, 자부담 비용, 총 비용과 누적 요약 제공

---

## v1.6.251 — 참석자 명단 호버·클릭 동시 제공

- 행사별 참석 인원과 클럽별 참석 인원(중복 제외) 차트에서 마우스 오버 시 이름·사번 명단을 다시 표시
- 막대 클릭 시 명단 팝업 열기와 `명단 복사` 기능은 그대로 유지

---

## v1.6.250 — 최신 화면 강제 갱신 보완

- 이전 서비스워커가 최신 버전을 감지한 뒤에도 과거 HTML·JS·CSS를 받는 캐시 경로 보완
- 앱 화면 파일은 서비스워커에서 HTTP 캐시를 우회해 항상 서버의 최신 파일을 확인하도록 변경

---

## v1.6.249 — 참석 인원 차트 명단 팝업·복사

- 긴 이름·사번 목록이 차트 툴팁 아래에서 잘리던 문제를 해결
- 행사별 참석 인원과 클럽별 참석 인원(중복 제외) 차트의 막대를 클릭하면 스크롤 가능한 참석자 명단 팝업 표시
- 팝업에서 이름·사번 명단을 번호와 함께 복사 가능

---

## v1.6.248 — 참석 인원 차트에 이름·사번 표시

- 행사별 참석 인원 차트의 막대에 마우스를 올리면 해당 행사 참석자의 이름과 사번을 표시
- 클럽별 참석 인원(중복 제외) 차트는 올해 해당 클럽의 고유 참석자 이름과 사번을 표시
- 인원이 많은 경우에도 읽기 쉽도록 참석자 목록을 툴팁에서 두 명씩 묶어 표시

---

## v1.6.247 — 관리자 시작 안내 토글 방식 개선

- 시작 안내에서 전사원 명부 관리 항목을 제외하고, 클럽·예산 설정 / 정산 기준 확인 / 정산 현황 확인의 3개 항목으로 간소화
- 클럽·예산 설정과 정산 기준 확인을 다시 누르면 각각의 설정 카드가 접히도록 토글 동작 추가
- 현재 열려 있는 설정은 `접기` 표시와 강조색으로 구분

---

## v1.6.246 — 관리자 첫 화면 시작 안내 추가

- 대시보드 상단에 `무엇부터 할까요?` 4단계 바로가기(클럽·예산 / 정산 기준 / 명부 / 정산 현황)를 추가
- 클럽·예산과 정산 기준 바로가기는 접힌 카드를 자동으로 열고 입력 위치로 이동
- 클럽 등록 전에는 설정 필요 상태를, 등록 후에는 현재 클럽 수를 표시해 신규 관리자가 다음 행동을 알 수 있도록 개선

---

## v1.6.245 — 행사별 참석 인원 차트 레이블 누락 수정

- 표시 대상을 최신순 최근 15건에서 최근 10건으로 줄여 차트 행 간격을 확보
- Chart.js 세로축 레이블 자동 생략을 끄고, 모든 막대 왼쪽에 `클럽명 날짜`가 표시되도록 수정

---

## v1.6.244 — 참석자 중복 안내를 입력란 옆 문구로 개선

- 동일 사번 중복 등록 시 브라우저 기본 알림 대신 이름 입력란 옆에 빨간 `⚠ 이미 등록됨` 문구와 기존 등록자 사번을 표시
- 이름 또는 사번을 수정하거나 수정 취소하면 경고 문구와 빨간 입력 테두리를 즉시 해제

---

## v1.6.243 — 현재 참석자 사번 중복 등록 차단

- 현재 참석자 추가·수정 시 이름이 달라도 같은 사번이면 중복으로 판단해 저장하지 않고, 이미 등록된 이름·사번을 팝업으로 안내
- 입력 폼뿐 아니라 `addAttendee` 함수 자체에서 검사해 다른 호출 경로에서도 중복 참석자가 생성되지 않도록 보완

---

## v1.6.242 — 저용량 사진·영수증 서버 보관 및 보존 기한 정리

- **저용량 서버 저장**: 행사 사진은 긴 변 960px·목표 70KB, 영수증은 긴 변 1280px·목표 120KB 수준의 JPEG로 변환한 뒤 Firebase에 저장. 한 정산의 첨부 합계는 Base64 기준 6MB로 제한
- **중복 저장 제거**: 확정 이력의 첨부 원본은 `globalHistory`에만 두고, 개인 정산 이력에는 메타데이터만 보관. 과거 엑셀 다운로드·수정 시에는 전체 이력 원본을 불러와 첨부를 복원
- **서버 저장 상태 안내**: 행사 사진·영수증 처리 중 용량과 서버 동기화 성공·실패를 화면에 표시해, 저장 실패가 조용히 지나가지 않도록 보완
- **보존 기한 정리**: 정산 연도 다음 해 4월 1일 이후 해당 이력의 행사 사진·영수증만 삭제. 금액·참석자·비용 항목은 보존하며, 전체 이력 정리는 관리자 로그인 시 자동 실행

---

## v1.6.241 — 관리자 이력 정렬 및 연간 예산 차트 연도 보정

- **관리자 이력 정렬**: 정산 등록 시각(`id`)이 아니라 실제 정산일(`settlementDate`) 기준 최신순으로 표시하고,
  정산일이 같을 때만 저장 시각을 보조 정렬 기준으로 사용. 구버전 이력은 등록 날짜·ID 순으로 호환
- **연간 예산 연도 필터**: 클럽별 예산 소진율과 예산 통계에서 과거 연도 정산을 제외하고 현재 연도
  `finalSupportAmount`만 집계
- **기존 사용액 포함**: 연간 사용액을 관리자가 설정한 `priorUsed`(올해 기존 사용)와 올해 정산 지원금의
  합으로 통일해 클럽 관리 화면의 잔여 예산과 일치시킴

---

## v1.6.240 — 이력 명부 누적 동기화 및 행사 사진 보존

- **명부 누적 재집계**: 정산 이력 참석자를 수정하거나 이력을 삭제하면 단순히 이름별 `count`를 가감하지
  않고, 남은 현재 연도 이력 전체를 사번(`employeeId`) 기준으로 다시 계산해 `count`와 `counts[사번]`을 동기화
- **관리자·작성자 동시 반영**: 관리자가 다른 사용자의 이력을 수정·삭제할 때 공유 이력뿐 아니라 작성자
  개인 이력과 개인 명부, 관리자 전체 명부까지 Firebase 원자 저장 한 번으로 함께 갱신
- **행사 사진 이력 보존**: 신규·수정 이력에 `eventPhotos`를 저장하고 수정 화면에서도 원본 사진을 복원
- **과거 엑셀 사진 격리**: 이력 엑셀 생성 중에는 해당 이력 사진만 사용하고 완료·실패 후 현재 세션 사진을
  반드시 복원. 사진 필드가 없는 구버전 이력은 빈 사진으로 처리해 다른 정산 사진 혼입 방지

---

## v1.6.239 — 정산 저장 원자성 및 실패 처리 강화

- **정산 확정 원자 저장**: 공유 정산 이력, 작성자 개인 이력, 개인 세션 초기화, 클럽 사용 예산·상품비
  누적액을 Firebase 다중 경로 `update` 한 번으로 저장하도록 변경
- **동시 저장 안전성**: 클럽 `usedBudget`·`prizeUsed`는 클라이언트의 오래된 절대값으로 덮어쓰지 않고
  Firebase 서버 증분을 사용해 여러 사용자의 동시 정산·수정 금액을 모두 보존
- **성공 후 초기화**: 원격 저장이 완료된 뒤에만 비용 항목·참석자·사진을 초기화하며, 네트워크 저장 실패
  시에는 현재 입력과 로컬 이력을 그대로 유지해 재시도 가능
- **수정 실패 전파**: 이력 수정 저장 오류를 호출 화면까지 전달해 실패 후 성공 안내·수정모드 해제·화면
  초기화가 실행되지 않도록 수정

---

## v1.6.238 — 모바일 가입 회원 관리 배치 개선

- 데스크톱의 5열 표는 그대로 유지하고, 600px 이하 화면에서만 회원 1명당 카드형 행으로 전환
- 이름과 PIN은 첫 줄에 배치하고, 가입일·최근 접속은 전체 폭의 라벨/값 구조로 표시해 날짜가 한 글자씩
  세로 줄바꿈되던 문제 수정
- 수정·삭제 버튼을 카드 오른쪽 아래 한 줄에 고정해 좁은 iPhone 화면에서도 버튼이 잘리거나 겹치지 않도록 개선

---

## v1.6.237 — 클럽별 정산 이력 필터·상품비 표시 및 누적 복구 수정

- **다른 클럽 이력 노출 수정**: 일반 사용자 정산 이력에서 현재 클럽의 전체 이력이 0건이면 개인 이력
  전체로 되돌아가던 폴백을 제거하고, 어떤 데이터 경로에서도 현재 `clubId`/클럽명과 일치하는 이력만
  표시하도록 수정
- **상품비 요약 표시**: 상품비가 포함된 정산 이력은 접힌 상태의 간단 요약에도 상품비 금액을 별도 표시
  (일반 사용자·관리자 이력 공통)
- **상품비 누적 0원 원인 수정**: 기존 이력에는 `prizeCost` 필드가 저장되지 않았는데 관리자 재집계가
  이 필드만 합산해 `clubRegistry.prizeUsed`를 0원으로 덮어쓰던 문제 수정. 구버전 이력은
  `expenseItems`의 `PRIZE` 항목을 합산하고, 신규·수정 이력에는 `prizeCost`를 명시 저장
- **기존 누적 자동 복구**: 일반 사용자 로그인 시 현재 연도 globalHistory의 상품비 합계가 레지스트리
  누적액보다 큰 경우에만 공유 누적액을 상향 복구 — 동시에 생성 중인 최신 정산액을 낮추는 덮어쓰기 방지

---

## v1.6.236 — "행사별 참석 인원" 차트 가로 목록형으로 개편

- **요청**: 세로 막대(날짜순, 색으로만 클럽 구분)가 "한눈에 보기 어렵다" — 원하는 읽기는
  "**어떤 클럽이 · 며칠 행사에 · 몇 명** 참석했다"
- **변경**: 행사 1건 = 1줄인 가로 목록형으로 개편 — 줄마다 왼쪽에 "클럽명 M/D" 라벨, 막대 길이 =
  참석 인원수, 끝에 "N명" 표시. 호버 없이도 세 정보가 한 줄에서 바로 읽힘
- 최신 행사가 맨 위(최신순), 최근 15건까지 표시. 막대 색은 클럽 고유 색 유지
- 스타일 후보 3가지(가로 목록형/클럽별 꺾은선/클럽별 묶음 막대)를 미리보기로 제시했고 사용자가
  가로 목록형을 선택함

---

## v1.6.235 — 가로 막대 차트 값 라벨 잘림 수정

- **현상**: "클럽별 참석 인원 (중복 제외)" 차트에서 값이 가장 큰 막대(예: 탁구사랑 28명)가 차트 오른쪽
  끝까지 차면서, 막대 끝 바깥에 그리던 값 라벨("28명")이 캔버스 밖으로 잘려 "2"만 보였음
- **수정**: `chartValueLabelPlugin`의 가로 막대 분기에서 라벨 폭을 실측(`measureText`)해 차트 영역
  오른쪽을 넘으면 **막대 안쪽에 우측 정렬로 자동 표시** — 글씨색은 막대 배경색의 명도에 따라
  흰색/어두운색을 자동 선택(`pickTextColorForBg`). 공간이 충분한 막대는 기존처럼 바깥에 표시
- 플러그인 공용 수정이라 정산 횟수·예산 소진율 등 모든 가로 막대 차트에 동일하게 적용됨
- Playwright로 28명 케이스 재현 → 막대 안쪽 "28명" 표시 확인

---

## v1.6.234 — 새 차트 2개 (행사별 참석 인원 / 클럽별 중복제외 참석자) + 추가 배정 Enter 적용

### 1. 차트 탭에 새 차트 2개 추가

- **행사별 참석 인원** (🙋): 정산(행사) 1건마다 참석한 인원수를 날짜순 세로 막대로 표시.
  막대 색은 클럽 고유 색(`clubColorFor`), 막대 위에 "N명" 라벨, 호버 시 "클럽명 · 날짜" 표시.
  상단 클럽 필터(`selectedOverallClubs`) 적용.
- **클럽별 참석 인원 (중복 제외)** (👥): 올해 각 클럽 행사에 참석한 **서로 다른 인원수**를 많은 순
  가로 막대로 표시 — 같은 사람이 여러 번 참석해도 1명으로 카운트. 고유 인원 판정은 **사번
  (`employeeId`) 기준**(동명이인은 사번이 다르면 서로 다른 사람으로 정확히 구분), 사번이 없는
  참석자만 이름으로 폴백. 작년 이력은 제외(올해 기준).
- 두 차트 모두 기존 차트와 동일한 규칙 준수: `getClubColorMap()`/`clubColorFor()` 클럽 고유 색,
  `chartValueLabel` 값 라벨, `maintainAspectRatio:false` + `.chart-canvas-wrap`
- Playwright로 시드 데이터(2개 클럽·6개 행사·동명이인 포함) 구동 검증: 행사별 인원, 중복제외
  카운트(넷짱 5명 > 볼링 3명), 동명이인 사번 구분, 작년 제외 모두 정확함을 확인

### 2. 관리자 "추가 배정" 팝업 Enter 키 적용

- **요청**: "관리자에서 추가배정 누르고 금액 기입하고 엔터눌러도 적용되게 해줘"
- 금액 입력 후 **Enter = 확인**(즉시 적용), **Escape = 취소**. 기존엔 확인 버튼 클릭만 가능했음
- 겸사겸사: `setupCurrencyInput`이 팝업을 열 때마다 입력 포매터 리스너를 중복 등록하던 것을 1회만
  등록되도록 가드 추가
- **금액 연동 점검 결과(요청받은 확인)**: 추가 배정 → `clubRegistry.budget` 증가(기존
  usedBudget/prizeUsed/승인 설정 보존) → 잔여 예산·법인카드 자동 제안이 이 값을 직접 읽어 즉시
  반영됨. 구간별(4구간) 자부담 계산은 예산과 무관(rules만 사용)이라 영향 없음 — 문제 없음 확인

---

## v1.6.233 — 전체 코드 점검에서 발견된 4건 수정 + 상품비 잔여 한도 표시

사용자 요청("전체 코드 점검해보고 오류가 날수 있는 부분이 있는지 검토해줘")으로 전수 점검을 진행해
발견한 문제들을 승인받아 일괄 수정.

### 1. 상품비 연 50만원 한도 검증 우회 구멍 수정 (가장 중요)

- **문제**: `clearClubData()`(클럽 전환 시)와 `resetSettlementSession()`(정산 초기화·이력 수정
  완료/취소 후)이 `previousPrizeTotal`(클럽의 올해 상품비 확정 누적액)을 무조건 0으로 지워버림.
  클럽 전환 핸들러는 올바른 값을 세팅한 바로 다음 줄의 `clearClubData()`가 도로 0으로 덮어쓰는
  구조였음. 이 상태에서 상품비를 등록하면 한도 검증이 "누적 0원" 기준으로 계산돼 **연 50만원
  한도를 초과하는 등록이 통과**될 수 있었음 (clubRegistry 스냅샷이 다시 도착할 때까지의 창)
- **수정**: 두 함수 모두 `previousPrizeTotal`을 0이 아니라 `clubRegistry[clubId].prizeUsed`(클럽
  확정 누적)로 복원하도록 변경 — `finalizeSettlement()`가 이미 쓰던 방식과 통일

### 2. 상품비 잔여 한도 표시 추가

- "기존 상품비 누적액" 입력칸 아래에 "🎁 올해 상품비 잔여 한도: N원" 표시 추가
  (연 한도 − 클럽 확정 누적 − 이번 세션 상품비, `render()`마다 갱신, 0원이면 빨간색)

### 3. 시설·장비 법인카드 인당 한도 하드캡 (수동 입력 차단)

- **문제**: 인당 85,000원 × 인원수 한도가 자동 제안값에만 적용되고, 법인카드 입력칸에 손으로 더 큰
  금액을 타이핑하면 예산 검증만 통과하면 그대로 등록됐음
- **수정**: 항목 추가 시 시설비 법인카드 지정액이 한도를 초과하면 차단 팝업 표시 — 확인 시 법인카드
  금액이 한도로 자동 조정되고 초과분은 개인카드(자부담) 처리. 한도는 **정산 1건 전체 기준**으로,
  같은 세션의 다른 시설비 항목에 이미 배정된 법인카드와 합산해 검사 (자동 제안값도 동일하게 잔여
  한도만 제안). 안내 팝업 문구도 "법인카드 사용은 인당 85,000원을 초과할 수 없습니다"로 교체

### 4. 오프라인 회원가입 무반응 수정

- Firebase 미초기화(오프라인) 상태에서 가입 버튼을 누르면 TypeError로 조용히 죽던 것을
  "인터넷 연결 후 가입할 수 있습니다" 안내로 교체. PIN 중복 확인 네트워크 오류 시에도 안내 추가

### 5. .gitignore 추가

- `node_modules/`(테스트용 Playwright 설치분) 등 로컬 개발 파일이 `git add -A` 시 배포 저장소에
  통째로 커밋되는 사고를 막기 위해 `.gitignore` 신설

---

## v1.6.232 — 시설·장비 이용료 계산 방식 최종 정리 (인당 flat 한도, 풀링 롤백)

- **배경**: v1.6.228~229에서 "시설비 기본값 = 행사비와 같은 인당 지원 한도 풀에 합산" 방식으로
  구현했었는데, 사용자가 실제 엑셀 구간표를 보다가 "시설비는 인원수와 무관한 정액성 비용인데, 인당
  구간표를 적용하는 게 맞나?" 라는 의문을 제기함. 논의 끝에 "행사비는 인당 계산이 맞지만, 시설비는
  인원수와 무관하게 계산해야 한다"는 결론에 도달
- **최종 결정**: 시설·장비 카테고리 선택 시 뜨던 체크박스("이사진 승인 한도 증액")와 2버튼 팝업(예/아니오)을
  전부 제거. 대신:
  - 카테고리 선택 시 **확인 버튼 하나뿐인 단순 안내 팝업**("시설 및 장비 이용에 대해 임원진의 승인을
    받으셨나요?")만 표시 — 계산에 전혀 영향을 주지 않는 알림용
  - 법인카드 제안액은 **항상** `인당 rules.facilityLimit(기본 85,000원) × 참석 인원수`까지만, 초과분은
    자동으로 개인카드(자부담) — 행사비 4구간 공식이나 다른 항목과의 풀링/합산은 완전히 제거
  - `item.facilityApproved` 필드, `_facilityApproved` 플래그, 체크박스 UI(`facility-boost-wrap`)를
    전부 제거 — 더 이상 "승인 여부"에 따른 계산 분기가 없어졌기 때문
- Node 시뮬레이션(4가지 케이스)과 Playwright 실제 브라우저 구동으로 재검증 완료. 리팩토링 과정에서
  발견된 `existingForCalc is not defined` 버그(v1.6.228/229의 풀링 코드 제거 시 변수명이 안 맞아 EVENT/
  PRIZE 항목의 법인카드 자동 제안이 깨지는 회귀)도 브라우저 콘솔 에러로 잡아서 함께 수정
- 관련 문서: `CALCULATION_SPEC.md` §5-4 전면 재작성, `CLAUDE.md` §1-6 표·§3, `APP_SPEC.md` §2

---

## v1.6.231 — 신규 가입 시 이름·PIN 모두 중복이면 "이미 가입되어 있습니다" 안내

- **요청**: "신규 가입할때 중복일경우 가입되어있습니다라는 문구가 나왔으면 좋겠어." → 확인 결과 "이름과
  PIN이 동일할 경우"를 의미
- **기존 동작**: PIN이 이미 등록돼 있으면 이름과 무관하게 항상 "이미 등록된 PIN 번호입니다. 다른 번호를
  입력해 주세요." 문구만 표시 — 본인이 실수로 재가입을 시도한 경우와 다른 사람이 우연히 같은 PIN을
  고른 진짜 충돌을 구분하지 못했음
- **수정**: 가입 시도한 PIN이 이미 존재할 때, 저장된 이름과 입력한 이름이 **둘 다 같으면** "이미 가입되어
  있습니다." 로 안내(본인 재가입 시도로 간주 — 로그인하라는 의미), **이름이 다르면** 기존 문구("다른
  번호를 입력해 주세요")를 그대로 유지(진짜 PIN 충돌)
- Playwright로 두 시나리오(이름+PIN 동일 / PIN만 동일) 모두 실제 브라우저에서 구동해 메시지가 정확히
  갈리는 것을 확인

---

## v1.6.230 — 정산 이력 "수정 취소"도 팝업 없이 바로 정산 초기화

- **요청**: "정산이력 수정하기 한다음 취소했을경우에는 비용정산에 나온 내용 바로 초기화 되게 해줘 이건
  팝업창 안나와도 되" — v1.6.229에서 "수정 완료" 저장 후에는 자동 초기화되도록 고쳤지만, "수정 취소"
  버튼은 그대로 두어 취소 시에는 여전히 이전 이력 내용이 화면에 남아있었음
- **수정**: `edit-mode-cancel-btn` 핸들러에서 `AppState.cancelEditMode()` 뒤에 `resetSettlementSession()`을
  호출하도록 변경 — 확인 팝업 없이(사용자가 명시적으로 요청한 대로) 바로 비용 항목·참석자·인원수가 비워짐

---

## v1.6.229 — 정산 이력 수정 후 자동 초기화 + 시설비 기본 계산 풀링 수정

### 1. 정산 이력 수정 완료 후 "정산 초기화" 자동 실행

- **요청**: "정산 이력에서 수정 누르고 내용 수정한다음 수정 완료를 누르면 관련 수정사항이 저장되는데
  그다음 정산 초기화 되어야할꺼같아 비용정산에 내용이 그대로 남아있어 불편해"
- **원인**: `edit-mode-done-btn`("✅ 수정 완료") 핸들러가 이력 저장(`updateHistoryEntry`) 후
  `AppState.cancelEditMode()`만 호출했는데, 이 함수는 수정 모드 배너·날짜만 초기화할 뿐
  `expenseItems`/`attendees`/`memberCount` 등은 그대로 남겨둠 — 그래서 저장 후에도 "비용 정산" 화면에
  방금 수정한 이력의 항목들이 계속 떠 있었음
- **수정**: 기존 "정산 초기화" 버튼의 초기화 로직을 `resetSettlementSession()` 함수로 추출해 공용화하고,
  "수정 완료" 저장 성공 직후 이 함수를 호출하도록 변경 — 저장이 끝나면 "정산 초기화" 버튼을 누른 것과
  동일하게 비용 항목·참석자·인원수·상품비 누적액 등이 전부 깨끗하게 비워짐

### 2. 시설·장비 기본(미승인) 법인카드 제안액 — 단독 계산 → 풀링 계산으로 수정

- **발견 경위**: 사용자가 실사용 중(넷짱 클럽, 참석자 4명) 저녁식사 136,000원(행사비, 나구간 자부담
  적용 중) 항목이 이미 있는 상태에서 "pc방 이용" 15,000원(시설·장비, 미승인)을 추가했더니 법인카드가
  15,000원 전액으로 제안됨 — "2번(다른 항목과 합산해서 계산)이 맞는 것 같다"고 확인
- **원인**: v1.6.228에서 시설비 기본값을 "이 항목 하나만 놓고" 인당 자부담 4구간 공식을 적용하도록
  구현했었음 — 15,000원 ÷ 4명 = 3,750원/인은 가구간(≤30,000원)이라 자부담 0%, 그래서 이미 다른
  행사비로 자부담 구간이 올라간 상태와 무관하게 항상 100% 법인카드로 제안됐음
- **수정**: `_calcCorpForItem`에서 미승인 시설비를 **행사비(EVENT)와 같은 "인당 지원 한도 풀"에
  합산**해서 계산하도록 변경 — 이번 세션의 다른 행사비/미승인 시설비 항목들을 계산용으로만 EVENT로
  취급해 합친 뒤, 이 새 항목을 더했을 때/더하기 전의 지원금 차이(한계기여분)를 법인카드 제안액으로 사용
  (§5-3에서 이미 쓰던 marginal-delta 기법과 동일한 방식). 실제 저장되는 카테고리·엑셀
  `facilityCost` 집계는 전혀 바뀌지 않음 — 오직 법인카드 기본 제안값 계산에서만 이렇게 취급
  - 검증: 위 예시 그대로 대입하면 풀 전체 인당비용 (136,000+15,000)÷4=37,750원(나구간 유지) →
    한계기여분 12,000원이 법인카드로, 나머지 3,000원이 개인카드로 정확히 배정됨을 Node 시뮬레이션과
    실제 브라우저 구동으로 모두 확인
  - 승인(85,000원 flat 한도)된 시설비 항목과 상품비(PRIZE)는 이 풀 계산에서 계속 제외됨 (변경 없음)
- 관련 문서: `CALCULATION_SPEC.md` §5-4, `CLAUDE.md` §1-6 표·§3

---

## v1.6.228 — 시설·장비 이용료 작동 방식 개선: 기본값 인당 지원 한도 + 승인 시 한도 증액

- **요청**: "처음에 시설 및 장비이용료 선택했을경우에는 기본 인당 지원한도까지 사용가능하게 적용해주고.
  대신 시설및 장비 이용료 선택했을때 자체 한도 85000원 증가 체크박스나 설정 버튼 만들어줘." — 기존에는
  카테고리를 "시설 및 장비 이용료"로 선택하는 즉시 이사진 승인 여부를 묻는 팝업이 강제로 떴고, "아니오"를
  누르면 카테고리 자체가 행사비로 바뀌어버렸음. 이를 개선해달라는 요청
- **변경 전**: 시설·장비 선택 → 즉시 팝업 → 승인(예) 시 법인카드 한도가 **인당이 아닌 항목 전체 기준
  85,000원 flat**으로 고정, 미승인(아니오) 시 카테고리가 행사비로 강제 전환됨
- **변경 후**:
  - 시설·장비 선택 시 팝업 없이 **기본값**이 바로 적용됨 — 이 항목 하나만 놓고 행사비와 동일한 인당
    자부담 4구간 공식을 적용해 "인당 지원 한도"만큼 법인카드를 기본 제안 (카테고리는 그대로 시설·장비 유지)
  - 항목 추가 폼에 새로 생긴 **"이사진 승인 한도 증액 (인당 85,000원)" 체크박스**를 체크하면 그때 확인
    팝업이 뜸: "시설 및 장비 이용료는 이사진의 승인이 있어야 인당 85,000원까지 사용이 가능합니다.
    승인 받으셨습니까?" — 예(인당 최대 85,000원까지 증액) / 아니오(행사비 방식과 동일하게 계산,
    체크박스 자동 해제)
  - 승인 시 법인카드 한도가 **인당 85,000원 × 참석 인원수**로 스케일됨 (기존의 항목 전체 기준 flat
    85,000원보다 실제 회사 지원 취지에 더 부합)
  - 항목별로 승인 여부(`item.facilityApproved`)를 저장해, 나중에 그 항목을 다시 수정할 때 체크박스
    상태를 정확히 복원 — 이전에는 "카테고리가 시설·장비면 무조건 승인된 것"으로 간주했던 부정확한 로직
- 두 값 모두 클럽 잔여 예산 한도(`realRemaining`)는 동일하게 적용되어 예산 초과는 여전히 불가능
- `CALCULATION_SPEC.md` §5-4, `CLAUDE.md` §3, `APP_SPEC.md` §2에 새 로직 문서화

---

## v1.6.227 — 시설·장비 100만원 초과 경고 문구 삭제

- **경위**: 사용자가 "시설 및 장비 이용료가 1,000,000원을 초과하여 별도 협의가 필요합니다" 경고를 보고
  본인이 이 요구사항을 넣은 기억이 없다고 확인 요청. 이 세션의 대화 기록 전체와 이전에 작업했던
  Antigravity 쪽에도 이 요구사항을 넣은 기록이 없음을 확인
- **조사**: git 최초 커밋(2026-06-10) 시점부터 이미 `app.js`에 이 경고 코드가 존재했음 — 그 이전
  단계(Claude Code 작업 시작 전)에 이미 완성된 앱 상태로 커밋됐기 때문에 도입 배경을 코드/문서/대화
  기록 어디에서도 추적할 수 없었음. 상품비(연 50만원 한도, 항목 추가 자체를 차단)와는 별개의, 처음부터
  독립적으로 존재한 규칙이었음(복사·붙여넣기 실수 아님)이 확인됨
- **결정**: 근거를 찾을 수 없는 규칙이라 사용자 요청으로 삭제. `SettlementCalculator.calculate()`의
  `warnings` 배열에서 `facilityCost > 1,000,000` 조건 자체를 제거 — 정산 결과 화면의 "주의사항 및
  한도 경고" 카드에 더 이상 이 문구가 뜨지 않음. 시설·장비 비용 자체 합계(`facilityCost`)나 관련 계산·
  사전승인(85,000원 법인카드 한도 등)은 이 항목과 무관해 변경 없음
- 관련 문서(`APP_SPEC.md`, `CALCULATION_SPEC.md`, `계산식_명세서.md`)에서도 해당 경고 조건 삭제

---

## v1.6.226 — 대시보드 차트 색상을 클럽 고유 색으로 통일

- **요청**: "차트 색상은 해당 클럽의 색상이 전부 매치되게해줘" — 월별 지출 현황·예산 소진율·클럽별
  정산 횟수 차트가 각자 자기 차트 안에서의 정렬 순서(알파벳순/소진율순/횟수순)로 색상 인덱스를 매겨서,
  같은 클럽인데도 차트마다 색이 달라 한눈에 매칭이 안 되는 문제
- **수정**: 전체 클럽 레지스트리를 이름순으로 고정 정렬해 클럽명→색상 매핑을 한 번만 만드는
  `getClubColorMap()`/`clubColorFor()`를 새로 만들고, 3개 차트(월별 지출 현황·예산 소진율·클럽별
  정산 횟수)가 전부 이 매핑을 참조하도록 교체 — 각 차트의 정렬/필터 상태와 무관하게 같은 클럽은
  항상 같은 색
- **예산 소진율 차트 추가 결정**: 기존에는 막대색으로 위험도(안전/주의/초과)를 표시했는데, 클럽색으로
  통일하면 그 신호가 사라짐 → 사용자 확인 후 막대는 클럽색으로 바꾸고, 대신 막대 위 소진율 % **숫자
  글씨색**을 안전(초록)/주의(노랑, 80%↑)/초과(빨강, 100%↑)로 바꿔 위험 신호를 유지
- `chartValueLabelPlugin`(가로 막대 값 표시 플러그인)에 `getColor(i)` 옵션을 추가해 항목별로 다른
  라벨 글씨색을 지정할 수 있도록 확장

---

## v1.6.225 — 클럽별 예산 소진율 차트 호버 내용에 잔여 예산 추가 + 순서 정리

- **요청**: "클럽별 예산 소진율" 차트에 마우스를 올렸을 때 예산·사용·잔여가 함께 표시되면 좋겠음
- **수정**: 툴팁 표시 순서를 **소진율 → 예산 → 사용 → 잔여** 순으로 정리, 그동안 빠져 있던
  "잔여" 항목(배정 예산 − (올해 기존 사용 + 올해 정산 지원금))을 추가

---

## v1.6.224 — 인앱 "업데이트 내역" 누락분 추가 + 버전 상수 이중관리 문제 발견·수정

- **발견한 문제**: `app.js` 최상단의 `APP_VERSION`/`APP_VERSION_DATE` 상수(로그인 화면의 "업데이트 내역"
  모달이 참조하는 유일한 데이터 소스)가 **v1.6.187에서 그대로 멈춰있었음** — 지금까지 배포 워크플로에서
  `sw.js`의 `APP_VERSION`만 계속 올려왔고, `app.js` 내부의 이 상수는 별개로 존재하는지 모른 채 누락되어
  왔음 (v1.6.207에서 "버전 라벨을 서버 기준으로 표시"하도록 fetch 기반 보정을 추가해 화면 표시는
  정상으로 보였지만, "업데이트 내역" 모달 목록은 이 상수를 그대로 써서 v1.6.183 이후 37개 버전
  — 1.6.184~223 — 이 목록에서 완전히 빠져 있었음)
- **수정**: `app.js`의 `APP_VERSION`/`APP_VERSION_DATE`를 `sw.js`와 동일하게 현재 버전으로 재동기화하고,
  `CHANGELOG` 배열에 v1.6.184~223의 41개 버전 항목을 모두 추가 — "업데이트 내역" 모달에서 전체 이력을
  다시 확인할 수 있음
- **재발 방지**: CLAUDE.md 작업 워크플로에 "버전 bump는 `sw.js` + `app.js` 상수 2곳 모두 필수" +
  "CHANGELOG 배열에도 새 버전 한 줄 추가" 절차를 명시

---

## v1.6.223 — 전사원 명부, 동명이인 중 미참석자가 참석자 위치에 딸려오던 문제 수정

- **현상**: 동명이인(예: 이진호 4277/4035/2711) 중 한 명(4035)만 실제로 참석했는데, 참석 안 한
  나머지 사번(4277, 2711)도 명부 목록에서 참석한 사람 바로 옆(위쪽 높은 순위)에 함께 표시됨
- **원인**: 정렬 기준이 "이름"(예: '이진호') 단위 합산 카운트였음 — 이름을 먼저 참석 횟수 합계로
  정렬한 뒤, 그 이름에 딸린 사번들을 순서 그대로(정렬 없이) 통째로 나열해서, 실제로는 0회인 사번도
  같은 이름의 합산 순위를 그대로 물려받았음
- **수정**: 정렬 단위를 "이름"에서 "사번(각 행)"으로 변경 — 동명이인이어도 사번마다 각자의 실제
  참석 횟수만으로 순위를 매겨서, 참석 안 한 사번은 이제 정직하게 하위(0회 구간)로 내려감

---

## v1.6.222 — 툴팁이 스크롤 영역에 잘리던 문제 수정 + 클럽 관리 버튼에 설명 추가

- **현상**: v1.6.221에서 추가한 "85K 미승인" 버튼의 호버 설명이 클럽 목록의 스크롤 영역 경계에서
  잘려 보임 (CSS `:hover` 기반 툴팁은 부모의 `overflow:auto` 스크롤 영역을 벗어날 수 없음)
- **수정**: 전역 커스텀 툴팁 시스템으로 교체 — `[data-tooltip]` 속성이 있는 요소에 마우스를 올리면
  JS가 `document.body` 기준 `position:fixed`로 툴팁을 배치해, 어떤 스크롤 컨테이너 안에 있어도 잘리지
  않고 완전히 표시됨. 위쪽 공간이 부족하면 자동으로 아래쪽에 표시
- **설명 추가**: "클럽 관리" 목록의 **추가 배정** 버튼, **올해 기존 사용** 입력칸에도 마우스를 올리면
  각각 무슨 기능인지 설명이 뜨도록 추가

---

## v1.6.221 — 85,000원 초과 승인 버튼 UI 개선 + 1회성 승인으로 변경

- **버튼 크기 축소**: "85,000원 초과 미승인/승인됨" 버튼을 다른 버튼보다 작게(글자 크기·여백 축소,
  라벨을 "85K 미승인"/"85K 승인됨"으로 축약)
- **호버 툴팁 추가**: 버튼에 마우스를 올리면 즉시 나타나는 커스텀 설명 툴팁 추가 (네이티브 `title`
  속성 대신 스타일이 적용된 말풍선 — `style.css`의 `.info-tooltip-wrap`/`.info-tooltip-text`)
- **1회성 승인으로 변경**: 관리자가 이 버튼으로 승인해도 **영구 설정이 아니라 다음 정산 1건에만
  유효** — 그 클럽의 정산이 확정되면(`finalizeSettlement`) 실제로 85,000원 초과분을 사용했는지와
  무관하게 자동으로 "미승인" 상태로 되돌아감. 다음에 또 필요하면 관리자가 다시 승인해야 함
- 승인 시 확인 팝업 문구도 "다음 정산 1건에 한해"임을 명시하도록 수정

---

## v1.6.220 — 인당 85,000원 초과 지원을 관리자 클럽별 사전승인 방식으로 변경

- **배경**: v1.6.219의 "심각" 단계(인당 85,000원 자체 초과)는 극히 예외적인 상황이라, 정산 화면에서
  사용자가 그 자리에서 팝업으로 직접 승인하는 방식은 적절하지 않음 — 관리자가 클럽 단위로 미리
  승인해두는 방식으로 변경
- **관리자 화면 추가**: "클럽 관리" 목록의 각 클럽에 **🔒 85,000원 초과 미승인 / 🔓 85,000원 초과
  승인됨** 토글 버튼 추가 (확인 팝업 후 전환). 이 설정은 클럽별로 독립적으로 저장됨(`clubRegistry.
  {clubId}.allowOverLimitSupport`)
- **정산 화면 동작 변경**:
  - 인당 법인카드 지원액이 **정책 계산값 초과 ~ 85,000원 이내**: 기존과 동일하게 그 자리에서 이사진
    승인 확인 팝업(주의, 주황) 표시
  - 인당 법인카드 지원액이 **85,000원 자체를 초과**: 팝업 없이,
    - 해당 클럽이 관리자로부터 승인된 경우 → 클럽 잔여 예산 내 최대 금액으로 자동 재계산 후 진행
    - 승인되지 않은 경우 → "관리자로부터 승인받지 않았습니다" 안내 후 정책 계산값으로 자동 원복
  - 두 경우 모두 클럽 잔여 예산 자체는 초과할 수 없음(하드 캡 유지)
- `addOrUpdateClub()` 호출 시 `allowOverLimitSupport` 값이 유실되지 않도록 보존 처리 추가

---

## v1.6.219 — 인당 지원 한도 초과 확인을 2단계(주의/심각)로 재설계

- **배경**: v1.6.218에서 처음 구현한 트리거 조건("인당 법인카드 합계가 85,000원 초과")이 실제 의도와
  달랐음 — 85,000원은 구간표상 절대 최대치라 정상적으로는 거의 도달하지 않고, 실제로 확인하고 싶은
  상황은 "예산은 충분하지만, 참석 인원 대비 **정책상 계산되는 지원액**보다 많이 법인카드로 배정하려는
  경우"였음
- **트리거 조건 변경**: (기존 항목 법인카드 합계 + 이번 항목 법인카드) ÷ 인원이,
  - **정책 계산값(구간표 기준 실제 행사비로 계산한 인당 지원액)을 초과** ~ 85,000원 이내 →
    **1단계(주의)** 팝업 — 주황색 경고 테마, "참석 인원 대비 정책상 지원 가능한 금액을 초과합니다.
    이사진 승인 하에 인당 최대 85,000원까지 지원받기로 하였나요?"
  - **85,000원 자체를 초과** → **2단계(심각)** 팝업 — 빨간색 긴급 테마, "인당 정책 최대 지원금을
    초과하는 금액입니다. 이사진 승인 하에 85,000원보다 많은 금액을 사용하기로 결정하셨나요?
    (단, 클럽 배정 잔여 예산은 초과할 수 없음)"
- **네/아니요 동작**:
  - 1단계 네 → 입력값 그대로 진행 (85,000원 이내이므로 별도 재계산 불필요)
  - 2단계 네 → 클럽 잔여 예산 내에서 사용 가능한 최대 금액으로 자동 재계산 (예산은 여전히 하드 캡)
  - 아니요(양쪽 공통) → 정책 계산값(구간표 기준)으로 법인카드 금액 자동 원복
- 승인 상태는 항목 추가 완료 시 또는 금액/법인카드 금액을 손으로 다시 바꾸면 초기화되어 재확인됨

---

## v1.6.218 — 행사비 인당 지원 한도(85,000원) 초과 시 이사진 승인 확인 팝업 추가

- **요청**: 법인카드로 배정하려는 금액이 참석 인원 대비 정책상 인당 최대지원금(85,000원)을 초과하면,
  "이사진 동의하에 인당 최대지원금 85,000원을 초과하여 지원받기로 하였나요?" 팝업을 강조 표시하고
  네/아니요로 처리
- **추가**: 행사비(EVENT) 항목 추가 시, (기존 항목들의 법인카드 합계 + 이번 항목 법인카드 지정액) ÷
  참석 인원이 인당 85,000원(관리자 설정 `rules.deduction4` 값)을 초과하면 확인 팝업 표시
  - **네** → 클럽 잔여 예산 내에서 법인카드로 사용 가능한 **최대 금액**을 계산해 입력칸에 채워줌
    (85,000원 한도는 무시하고 실제 예산만 기준으로 함)
  - **아니요** → 인당 85,000원 정책 한도에 맞춰 법인카드 금액을 자동으로 다시 계산해 되돌림
    (초과분은 개인카드=자부담으로 남음)
  - 승인은 그 항목 추가 1건에 한해서만 유효 — 카테고리 변경 시 또는 항목 추가 완료 후 자동 초기화
- 시설·장비(FACILITY)는 이미 동일한 개념의 자체 사전승인 흐름(v1.6.142)이 있어 이번 대상에서 제외

---

## v1.6.217 — 클럽 예산 소진 후 비용 항목 추가 자체가 막히던 문제 수정 (전액 자부담 처리 허용)

- **배경**: 특정 클럽이 이사진 승인 하에 배정 예산을 전부 소진하고, 초과분은 자부담으로 처리하는
  예외 운영을 하는 경우가 있음 — 배정 예산 자체를 올리는 게 아니라, "예산이 다 떨어지면 그 이후는
  전액 개인 부담"이 정상적인 흐름이어야 함
- **현상**: 클럽 예산이 소진된 상태에서 비용 항목을 새로 추가하려 하면, 개인카드(자부담)로 전액
  처리하려고 해도 "클럽 연간 예산이 모두 소진되었습니다. 더 이상 비용을 추가할 수 없습니다" 팝업으로
  항목 추가 자체가 완전히 차단됨
- **원인**: 항목 추가 시 예산 초과 검증이 "이 항목이 전액 지원된다면"이라는 정책(구간표) 추정치를
  기준으로 남은 예산과 비교했음 — v1.6.215에서 실제 자부담 기준이 항목별 법인/개인카드 실배분으로
  바뀌었는데, 이 검증 로직만 예전 방식(전체 항목 합계의 구간표 추정치)에 머물러 있었음
- **수정**: 예산 초과 검증 기준을 "이 항목에 실제로 배정하려는 법인카드 금액"으로 변경 —
  법인카드 금액을 0원(전액 개인카드)으로 두면 클럽 예산이 얼마가 남아있든 항목 추가가 항상 허용되고,
  법인카드에 실제로 배정하려는 금액이 진짜 남은 예산을 초과할 때만 차단됨
  (클럽별 예산 확인·전체 클럽 총 예산 확인 두 곳 모두 동일하게 수정)
- 상품비(PRIZE)는 정책상 100% 법인카드만 가능하므로 관련 검증은 그대로 유지(변경 없음)

---

## v1.6.216 — 예산 소진 후에도 신규 항목에 법인카드가 계속 제안되던 문제 수정

- **현상**: 클럽 예산을 앞선 항목에서 이미 전액(또는 정책 제안치보다 많이) 법인카드로 수동 조정해
  소진했는데도, 새 비용 항목을 추가하면 여전히 법인카드 금액이 자동 제안되는 문제
- **원인**: 신규 항목의 법인카드 자동 제안 로직(`_calcCorpForItem`)이 "이번 세션에서 다른 항목에 이미
  실제로 배정된 법인카드 금액"이 아니라, 구간표로 추정한 값만 기준으로 잔여 예산을 계산 — 관리자가
  특정 항목의 법인카드 금액을 정책 제안치보다 많이 수동으로 올려도 그 사실을 반영하지 못했음
  (v1.6.215에서 "실제" 자부담 기준이 항목 합계로 바뀌면서 이 괴리가 실사용 중 드러남)
- **수정**: 제안액을 "정책상 한계기여분"과 "실제 남은 예산(클럽 잔여예산 - 이번 세션에서 다른 항목에
  실제로 배정된 법인카드 합계)" 두 값으로 분리해 계산 후 더 작은 값을 사용하도록 변경
- CALCULATION_SPEC.md §5-3에 새 공식 문서화

---

## v1.6.215 — 항목별 법인/개인카드 금액이 실제 자부담·지원금에 반영되도록 계산 방식 변경

- **요청**: 비용 항목마다 지정하는 법인카드/개인카드 금액이 지금까지는 계산에 전혀 반영되지 않고
  엑셀 영수증 배치 위치 결정에만 쓰였는데, "항목별 개인카드 금액 = 그 항목의 자부담, 법인카드 금액 =
  그 항목의 회사 지원금"이 되어 전체 자부담·지원금·잔여예산이 그 합계로 결정되도록 변경
- **유지되는 것**: 4단계 구간표(가/나/다/라) 자동 계산은 그대로 남아있음 — 이제는 "정책상 최소 자부담"
  **참고값**으로만 쓰임 (엑셀 K20/K21/K22, "인당 자부담 비용" 표시). 공식 자체·검증기(`SettlementValidator`)·
  신규 항목 추가 시 자동 제안 로직(`_calcCorpForItem`)·상품비 개인카드 UI 제한(v1.6.129)은 전부 변경 없음.
- **바뀌는 것**: "실제" 자부담/지원금(최종 지원금 표시, 클럽 예산 차감, 엑셀 K24/K25/L25/K30, 이메일
  보고서)의 기본값이 구간표 계산값 → **항목별 개인카드 금액 합계(`itemSelfPay`)** 로 바뀜.
  - 시설비 85,000원 초과분(이미 자동으로 "개인카드"에 배정되고 있었음)이 이번에 처음으로 실제
    자부담에 포함됨 — 의도된 변경.
  - "총 자부담 금액 직접 수정" 입력창은 그대로 유지 — 항목 합계 위에 최종 수동 조정을 얹는 방식은 동일.
- **회귀 방지**: 전액 법인카드 처리된 항목(itemSelfPay=0)이 "아직 수정 안 함"과 혼동되어 구간표 값으로
  잘못 새던 문제를 막기 위해, `lastCalculatedSelfPay > 0` truthy 체크 대신 명시적 플래그
  `AppState.selfPayManuallyOverridden`을 도입 (항목 추가/삭제/정산 확정/세션 초기화 등에서 리셋).
- **알려진 한계**: 2026-06-10 이전(법인/개인카드 필드 도입 전) 저장된 이력을 다시 열어 수정하면 그
  항목들은 `personalAmount`가 없어 0으로 처리됨 — 아주 오래된 이력 수정 시에만 해당하는 좁은 예외.
- CALCULATION_SPEC.md에 새 공식과 검증용 예시값 3건을 함께 기록.

---

## v1.6.214 — 이력 수정 중 "잔여 예산 초과" 오탐 차단 + 수정 화면 예산 계산 방식 개선

- **현상**: 관리자가 정산 이력 수정 화면에서 비용 항목을 추가/변경하려 하면 "잔여 예산을 초과합니다"
  팝업이 뜨며 막히는 문제 (v1.6.213으로 저장 후 보정은 해결됐지만, 수정 중 실시간 검증에는
  여전히 같은 원인의 버그가 남아있었음)
- **원인**: `getClubUsedBudget()`(잔여 예산 계산의 기준 함수)이 "이미 사용한 금액"을 계산할 때
  **지금 수정 중인 이력 자신의 기존 금액도 포함**해서 계산 — 그 상태에서 이 항목의 새 합계를
  다시 비교하니 자기 자신과 충돌해 실제로는 예산이 남아 있어도 초과로 판정됨
- **수정**: `getClubUsedBudget()`이 이력 수정 모드(`editingHistoryId`)일 때는 **이 이력이 원래
  기여했던 지원금을 "이미 사용한 금액"에서 자동으로 제외**하도록 수정 — 한 곳만 고치면
  이 함수를 사용하는 모든 화면(정산 결과의 "클럽 잔여 예산", 비용 추가 시 예산 초과 검증 등)에
  동시에 적용됨
  - 상품비(PRIZE) 한도 검사도 동일한 원리로 `previousPrizeTotal`에서 이 이력의 기존 상품비를 제외
  - 전체 클럽 총 예산 초과 검사(clubRegistry.usedBudget 합산 방식)도 동일하게 보정
- **개선**: "총 자부담 금액 수동 수정" 시 표시되는 "이전 잔여 예산 → 이후 남은 잔여 금액" 계산을
  자동 계산 결과 화면과 동일한 `getClubBudget()`/`getClubUsedBudget()` 기준으로 통일
  (기존엔 별도의 레거시 변수를 사용해 두 화면의 숫자가 다르게 나올 수 있었음)
- **결과**: 이제 이력 수정 화면은 "원래 이 정산이 얼마였는지"를 자동으로 빼고 계산하므로,
  금액을 수정하면 잔여 예산이 정확히 그 **차액만큼만** 바뀐 것으로 표시·검증됨

---

## v1.6.213 — 정산 이력 수정 시 클럽 예산이 새 금액만큼 "추가"되던 문제 수정

- **현상**: 관리자가 정산 이력에 들어가 금액을 수정하면, 클럽의 "사용 예산"이 수정 전 금액을 뺀
  차액만큼만 반영되지 않고 마치 새 정산이 하나 더 생긴 것처럼 인식되는 문제
- **원인**: `finalizeSettlement()`(신규 정산 확정)는 확정 시 `clubRegistry.usedBudget`/`.prizeUsed`를
  해당 금액만큼 **증분**하는데, `updateHistoryEntry()`(이력 수정 저장)는 `globalHistory`의
  금액 필드만 덮어쓸 뿐 `clubRegistry`는 전혀 보정하지 않았음 — 관리자 대시보드를 다시 열어
  전체 재계산이 돌아야만 뒤늦게 맞춰지는 구조였고, 그 전까지는 수정 전 금액이 그대로 남아있어
  실제로는 "반영 안 됨"과 "더해진 것처럼 보임"이 뒤섞여 나타났음
- **수정**: `updateHistoryEntry()`에 **차액(delta) 기반 보정** 추가
  - 수정 전/후 `finalSupportAmount` 차이만큼 `clubRegistry.usedBudget` 조정
  - 수정 전/후 상품(PRIZE) 카테고리 비용 차이만큼 `clubRegistry.prizeUsed` 조정
  - 클럽 전체 이력 캐시(`clubHistory`)도 즉시 갱신 → 저장 직후 화면에 뜨는 잔여예산이 바로 정확한 값
- **부수 수정**: 이력 조회 대상을 본인 `settlementHistory`뿐 아니라 `clubHistory`(클럽 전체 이력)에서도
  찾도록 확장 — 관리자가 다른 회원이 작성한 이력을 수정할 때 조용히 저장이 안 되던 경우도 함께 해결
  (단, 다른 회원 개인 이력 배열 자체는 건드리지 않음 — 계정 간 데이터 병합 사고 재발 방지 원칙 유지)

---

## v1.6.212 — 참석자 검색 시 동명이인 중 "이 클럽 최근 참석" 사번 강조 표시

- **요청**: 참석자 이름 검색 시 동명이인(같은 이름, 다른 사번)이 나오면, 그중 현재 선택된 클럽에
  실제로 최근 참석한 이력이 있는 사번을 바로 구분할 수 있게
- **추가**: `AppState.getRecentClubAttendance(employeeId)` — 현재 클럽의 이력(`clubHistory`)에서
  해당 사번의 가장 최근 참석 날짜를 조회
  - **동명이인 선택 팝업** (이름 완전 일치 시 뜨는 사번 선택 모달): 최근 참석 이력이 있는 사번 버튼을
    초록 테두리·배경으로 강조, "✅ 이 클럽 최근 참석 (YYYY-MM-DD)" 표기
  - **인라인 자동완성 드롭다운** (부분 일치 목록에 동명이인이 섞여 있을 때도) 동일하게 초록 강조 +
    마우스 오버 시 최근 참석일 툴팁
  - 최근 이력이 없는 사번은 기존 스타일 그대로 유지 (주변 디자인 영향 없음)

---

## v1.6.211 — 차트에 값 직접 표시 (호버 불필요) + 범례 글씨 안 보이는 버그 수정

- **버그 수정**: v1.6.210에서 카테고리 도넛차트 범례에 커스텀 `generateLabels`를 추가하면서
  Chart.js v4가 요구하는 항목별 `fontColor` 필드를 빠뜨려 범례 글씨가 렌더링되지 않던 문제 수정
  (Chart.js v4 범례는 전역 `labels.color`가 아니라 각 항목의 `fontColor`로 텍스트를 그림)
- **개선**: "하단 범례만" 보던 방식에서 **차트 자체에 값이 바로 보이도록** 공용 플러그인(`chartValueLabel`) 추가,
  4개 차트 모두에 적용 (호버 없이 항상 표시):
  - **클럽별 월간 지출액 비교** (막대): 각 월 막대 맨 위에 합계 금액 표시 (1.6M / 850K 등 축약 표기)
  - **클럽별 연간 예산 사용 비율** (가로 막대): 막대 끝에 소진율(%) 표시
  - **카테고리별 비용 비중** (도넛): 조각이 충분히 크면 조각 안쪽에, 작으면(예: 20° 미만) 얇은
    연결선과 함께 조각 바깥쪽에 비율(%) 표시 — 조각 안쪽 글씨색은 채움색 밝기에 따라
    흰색/잉크색을 자동 선택해 항상 대비 확보
  - **클럽별 정산 횟수** (가로 막대): 막대 끝에 건수 표시
  - 막대 끝/캡 라벨이 축 경계에 잘리지 않도록 각 차트의 축 최대치에 여유(margin) 확보
  - 자부담 vs 지원금 추이(월별 선 그래프)는 데이터 포인트가 많아 값을 전부 표기하면
    오히려 가독성이 떨어지므로 이번 적용 대상에서 제외 (범례·툴팁으로 계속 확인)

---

## v1.6.210 — 카테고리별 비용 비중 차트, 범례에 금액·비율 직접 표시

- **현상**: 관리자 차트 탭의 "카테고리별 비용 비중" 도넛차트는 마우스를 올려야만(툴팁)
  행사비/시설비/상품비 금액과 비율을 확인할 수 있었음
- **수정**: 범례(legend) 텍스트 자체에 `라벨  금액 (비율%)` 형식으로 표시되도록
  `generateLabels` 커스터마이즈 — 호버 없이 바로 확인 가능
  - 범례 위치(하단)·색상·폰트 크기 등 기존 스타일 그대로 유지, 텍스트 내용만 확장
  - 마우스 오버 시 툴팁은 기존과 동일하게 유지 (동일 정보 재확인용)

---

## v1.6.209 — 클럽 목록에 "올해 기존 사용" 인라인 입력칸 추가

- **요청**: 수정 버튼을 거치지 않고, 클럽 목록에서 바로 현재까지 사용한 금액을 입력하면
  잔여 예산이 자동 계산되도록
- **추가**: 클럽 목록 각 행에 **"✏️ 올해 기존 사용"** 입력칸 신설 (배정 예산과 상품비 사용 사이)
  - 금액 입력 후 Enter 또는 다른 곳 클릭 → 즉시 저장 + 잔여 예산 자동 재계산
  - 0 입력 시 초기화, 쉼표 자동 표기
  - 저장은 기존 `priorUsed` 경로 그대로 (공유 clubRegistry) → 유저 정산 화면 잔여 예산에도 반영
- **보호 로직**: 입력 도중 Firebase 실시간 스냅샷 재렌더가 입력값을 지우지 않도록
  해당 칸에 포커스가 있으면 `renderClubManagement` 재렌더 스킵 (blur 시 저장 후 직접 재렌더)
- 배정 예산 아래 "(기존 사용 X원)" 소형 표기는 전용 입력칸으로 대체되어 제거

---

## v1.6.208 — 클럽 관리에 "올해 기존 사용 금액" 입력칸 추가

- **배경**: 연중(7월)부터 앱 사용 시작 — 그 전까지 클럽별로 이미 지출한 금액을 잔여 예산에서
  차감한 상태로 시작할 방법이 관리자 화면에 없었음 (기존에는 각 사용자가 정산 화면 설정에서 입력하는 안내만 존재)
- **추가**: 클럽 관리 폼(클럽 추가/수정)에 **"올해 기존 사용 금액 (원)"** 입력칸 신설
  - "수정" 버튼 클릭 시 해당 클럽의 기존 값이 자동으로 채워짐
  - 입력값이 있으면 그 값으로 갱신 (0 입력 시 초기화), **비워두면 기존 값 유지**
  - 저장 시 클럽별 `priorUsed`로 저장 → 관리자 화면 잔여 예산(`배정 - 기존사용 - 올해지원금`)과
    유저 정산 화면 잔여 예산(`getClubUsedBudget` = priorUsed + 이력 사용액)에 모두 즉시 반영
  - 클럽 목록의 배정 예산 아래 "(기존 사용 X원)" 표기는 기존 로직 그대로 활용
- 안내 문구 교체: "이전 사용 금액은 각 사용자가 직접 입력" → 관리자 폼에서 입력·변경하는 방식으로 안내

---

## v1.6.207 — 버전 라벨을 서버 배포 버전 기준으로 표시 (최종 안전망)

- **현상**: 첫 접속/F5마다 로그인 화면 버전이 v1.6.187(2026.07.02)로 표시, 캐시 초기화 때만 잠깐 최신
- **원인**: 라벨 갱신 경로가 전부 "활성 SW의 협조"에 의존 —
  활성 SW가 구버전이면 `getVersion` 질의에 응답하지 못하고(v1.6.203 이전 SW), `SW_UPDATED`도 activate 시점에만
  발송되므로, SW가 구버전에 고정된 기기에서는 라벨이 영원히 옛 localStorage 값(1.6.187)을 표시
- **수정**: 페이지 로드 시 서버의 `sw.js`를 직접 fetch(`?_=타임스탬프` + `no-store`)해 `APP_VERSION`을 파싱·표시
  → SW 상태·설치 실패·프록시 캐시와 무관하게 라벨은 항상 실제 배포 버전을 표시

---

## v1.6.206 — 명부 탭 버튼 카운트를 사번 기준 인원수로 통일

- **현상**: 동명이인 41건이 정상 병합됐는데도 상단 탭 버튼이 "전사원 명부 (1272명)" 그대로
- **원인**: 탭 버튼 카운트(`directory-badge-count`)는 `Object.keys(directory).length` = **이름 개수** 기준.
  동명이인 추가는 기존 이름에 사번만 늘리므로 이름 개수는 변하지 않음 (1272 그대로).
  반면 명부 탭 내부 헤더(`directory-count`)는 사번별 행 수 기준 → 두 카운트가 서로 불일치.
- **검증**: 실제 옛/새 JSON으로 `bulkImportDirectory` 병합 로직을 Node 시뮬레이션 —
  사번행 1,272 → **1,315** 정상 증가, 이미숙(3)·이상훈(4)·이진호(3) 사번 모두 정상 병합 확인.
  즉 데이터 병합은 v1.6.205 로그인 시점에 이미 성공했고, 버튼 표시 기준만 문제였음.
- **수정**: 탭 버튼 카운트도 사번 기준 인원수(ids 합산)로 변경 — 내부 카운트와 동일 기준

---

## v1.6.205 — 명부 JSON·SW 자산 캐싱 HTTP 캐시 완전 우회

- **현상**: v1.6.204 적용 후에도 재로그인 시 명부가 1,272명 그대로 (신규 41건 미반영)
- **원인**: GitHub Pages HTTP 캐시(max-age 600초)의 이중 함정 —
  1. 10:18 로그인 때 캐시된 옛 JSON이 10:28 재로그인에 그대로 재사용됨 (10분 이내)
  2. v1.6.204의 `no-cache` 수정이 실리려면 새 app.js가 필요한데, app.js 자체도 같은 10분 캐시에 걸려
     "SW 버전은 204인데 실행 중인 app.js는 구버전"인 창이 존재
- **수정**:
  - `app.js`: 명부 JSON fetch를 `?_=타임스탬프` + `cache: 'no-store'`로 변경 — 모든 캐시 계층 무조건 우회
  - `sw.js`: install 시 `cache.addAll`을 `Request(url, { cache: 'reload' })`로 변경 —
    새 SW가 설치될 때 항상 서버에서 최신 자산을 받아 캐시 (구버전 app.js가 새 캐시에 들어가는 것 방지)

---

## v1.6.204 — 전사원 명부 JSON 갱신이 즉시 반영되지 않는 문제 수정

- **현상**: v1.6.202에서 추가된 동명이인 41건이 배포됐는데도 명부 카운트가 1,272명 그대로
- **원인**: 명부 JSON(`employee_directory.json`)은 **로그인 시점에만** fetch → `bulkImportDirectory()` 병합되는데,
  GitHub Pages의 HTTP 캐시(max-age 600초) 또는 배포 전 로그인으로 **옛 JSON을 읽으면 병합할 게 없어 조용히 스킵**됨.
  이후 재로그인해도 캐시가 살아있으면 계속 옛 JSON 사용.
- **수정**: 명부 JSON fetch 3곳(관리자 로그인/일반 로그인/초기 로드)에 `{ cache: 'no-cache' }` 적용
  → 항상 서버에 재검증, 변경 시 즉시 새 명부 반영
- **적용 방법**: 앱 새로고침 후 **로그아웃 → 재로그인** 하면 명부가 1,315명(사번 기준)으로 갱신됨

---

## v1.6.203 — 버전 라벨이 구버전(1.6.187)에 고정되는 문제 수정

- **현상**: 캐시 초기화 시 버전이 잠깐 최신으로 표시됐다가 즉시 v1.6.187(2026.07.02)로 되돌아가 고정됨
- **원인**: 버전 라벨은 SW가 **activate 되는 순간** 보내는 `SW_UPDATED` 메시지로만 기록되는데,
  v1.6.202에서 추가된 `controllerchange → 즉시 reload()`가 이 메시지와 **경쟁(race)** —
  리로드가 먼저 실행되면 localStorage에 새 버전이 저장되기 전에 페이지가 사라지고,
  SW는 이미 활성화된 뒤라 메시지가 재전송되지 않아 라벨이 옛 값(1.6.187)에 영구 고정됨.
  (앱 코드/데이터는 네트워크 우선 전략이라 실제로는 최신이었음 — 라벨만 구버전 표시)
- **수정**:
  - `sw.js`: `getVersion` 질의 수신 시 `SW_VERSION` 메시지로 현재 버전 응답
  - `index.html`: 페이지 로드 시마다 활성 SW에 버전 질의 → 라벨을 항상 실제 SW 버전과 동기화
    (`SW_VERSION`은 라벨 갱신만, 업데이트 배너는 띄우지 않음)
  - 버전 저장·표시 로직을 `applyVersionInfo()` 공용 함수로 분리

---

## v1.6.202 — 전사원 명부 동명이인 전수 등록 + 캐시 자동 업데이트

### 전사원 명부 동명이인 추가 (40쌍, 41건)
`template.xlsx` "Global ID" 시트 원본 대조 결과, 기존 3쌍(김진욱·이현준·이진호)만 등록되어 있던 동명이인을 **40쌍으로 전수 보강**.

| # | 이름 | 사번 | 인원 |
|---|------|------|------|
| 1 | 유영종 | 4002, 0714 | 2명 |
| 2 | 이원재 | 1664, 1015 | 2명 |
| 3 | 최영기 | 1607, 1091 | 2명 |
| 4 | 이기영 | 3815, 1119 | 2명 |
| 5 | 김동식 | 2902, 1225 | 2명 |
| 6 | 정우현 | 1732, 1460 | 2명 |
| 7 | 이동수 | 4285, 1550 | 2명 |
| 8 | 김지호 | 2809, 1554 | 2명 |
| 9 | 김영근 | 3926, 1679 | 2명 |
| 10 | 이명선 | 3869, 1683 | 2명 |
| 11 | 김동현 | 2247, 1713 | 2명 |
| 12 | 이경호 | 2341, 1808 | 2명 |
| 13 | **이미숙** | **2169, 1813, 1929** | **3명** |
| 14 | 이소영 | 2610, 1839 | 2명 |
| 15 | 이은영 | 3045, 1885 | 2명 |
| 16 | 김태진 | 3924, 1945 | 2명 |
| 17 | 김태영 | 3724, 1964 | 2명 |
| 18 | 김경민 | 3021, 2122 | 2명 |
| 19 | 김수진 | 2220, 2192 | 2명 |
| 20 | 김현석 | 4527, 2241 | 2명 |
| 21 | 김동우 | 4047, 2323 | 2명 |
| 22 | 홍현진 | 3307, 2343 | 2명 |
| 23 | 오승준 | 3460, 2381 | 2명 |
| 24 | 김경훈 | 3576, 2477 | 2명 |
| 25 | 김윤정 | 2659, 2488 | 2명 |
| 26 | 김영석 | 3214, 2626 | 2명 |
| 27 | 이재민 | 4014, 2627 | 2명 |
| 28 | **이진호** | **4277, 4035, 2711** | **3명** |
| 29 | 김태섭 | 3335, 2761 | 2명 |
| 30 | 김정한 | 3935, 2910 | 2명 |
| 31 | 이은주 | 4061, 3017 | 2명 |
| 32 | 김지수 | 3391, 3039 | 2명 |
| 33 | 김진욱 | 4057, 3235 | 2명 |
| 34 | 최재영 | 4671, 3325 | 2명 |
| 35 | 정민석 | 3848, 3329 | 2명 |
| 36 | 김정훈 | 3776, 3459 | 2명 |
| 37 | 이명재 | 3507, 3480 | 2명 |
| 38 | **이상훈** | **4523, 3574, 3854, 3910** | **4명** |
| 39 | 이현준 | 4461, 3645 | 2명 |
| 40 | 이정훈 | 4669, 3729 | 2명 |

- `employee_directory.json`: 1,274건 → **1,315건** (41건 추가)
- 관리자 로그인 시 `bulkImportDirectory()`가 Firebase 명부에 자동 동기화

### 캐시 자동 업데이트 수정
- **현상**: 접속 시 항상 구버전(1.5.187)이 표시되고, 캐시 초기화 버튼을 눌러야 최신(1.6.201)으로 갱신됨
- **원인 1**: SW `install` 이벤트에서 `skipWaiting()`을 호출하지 않아 새 SW가 대기(waiting) 상태로 머물러 있었음
- **원인 2**: SW 등록 시 `updateViaCache` 미지정으로 브라우저 HTTP 캐시에서 구버전 `sw.js`를 재사용
- **원인 3**: 캐시 초기화 후 `location.reload(true)` (deprecated)가 실제로 HTTP 캐시를 무시하지 않음
- **수정**:
  - `sw.js`: `install` 이벤트에서 `self.skipWaiting()` 즉시 호출
  - `index.html`: SW 등록 시 `{ updateViaCache: 'none' }` 옵션 추가
  - `index.html`: `controllerchange` 이벤트 시 자동 `reload()` 추가
  - `app.js`: 캐시 초기화 후 cache-busting 쿼리스트링(`?_cb=timestamp`)으로 리다이렉트

---

## v1.6.201 — 전체 코드 검증(코드리뷰) 후속 수정

v1.6.197~200 작업분 전체를 다각도 코드리뷰로 검증한 결과 발견된 문제 수정.

### 버그 수정
- **이력 로드 전 Firebase 0원 덮어쓰기 사고 방지** (핵심):
  - `renderClubManagement()`가 globalHistory 로드 완료 **전**(빈 `lastHistoryList`)에 실행되면
    모든 클럽의 `prizeUsed`/`usedBudget`을 0으로 계산해 공유 `clubRegistry`에 **0을 써버리던 문제**
  - `historyLoaded` 플래그 추가 — globalHistory 최초 로드 완료 후에만 Firebase 동기화 쓰기 허용
  - 이것이 "잔여 예산이 가끔 안 보임" 증상의 근본 원인 (v1.6.199는 표시만 교정, 쓰기 사고는 잔존했음)
- **차트 예산 통계 타일 첫 로드 시 0원 표시**: `updateChartsBudgetStats`가 동기 블록에서만 호출되어
  로그인 후 첫 대시보드 진입 시 빈 이력 기준 0원으로 남던 문제 → globalHistory `.then()`에도 호출 추가
- **`_countAttendeesByEmpId` 상단 주석 정정**: 제거된 "이름 폴백" 동작을 여전히 설명하던 구식 주석
  → 실제 동작(사번 없으면 스킵) + 재도입 금지 경고로 교체 (주석만 수정, 로직 무변경)

### 개선
- **차트 3중 렌더 제거**: 차트 탭 클릭 시 renderAllCharts가 3회(동기 step 3 + rAF + `.then()`) 실행되며
  차트가 깜빡이던 문제 → step 3의 중복 호출 제거 (rAF 핸들러 + `.then()` 2경로가 담당)

### 검증 결과 이상 없음 확인 항목
- `CALCULATION_LOCKED` 영역(계산 로직)은 이번 작업분과 무관 — 미접촉 확인
- `.off('value')` 광역 해제로 인한 타 리스너 오삭제 없음 (clubRegistry value 리스너 등록처는 1곳뿐)
- `_buildIdToName`/`_countAttendeesByEmpId` 사번 String 변환 일관성 확인
- `recalculateDirectoryCountsFromGlobal`의 `this.render()`/`this.isLoggedIn` 유효성 확인

---

## v1.6.200 — 클럽 중복 생성 방지 + 클럽 목록 넘버링 추가

### 버그 수정
- **클럽 중복 생성 재발 원인 제거**: `loadClubRegistry()`가 탭 전환 때마다 새 Firebase `.on('value', ...)` 리스너를 누적 등록하던 문제 수정
  - `.on()` 등록 전 `.off('value')` 호출 추가 → 항상 리스너 1개만 유지
  - 복수 리스너가 동시에 `_autoDeduplicateClubs()`를 호출하며 Firebase에 충돌 write를 일으키던 경쟁조건 제거
- **`renderAdminDashboard`에서 `loadClubRegistry()` 재호출 제거**: 탭 전환 시 기존 실시간 리스너(`AppState.clubRegistry` 자동 최신화)를 재사용하도록 변경
  - 기존: `AppState.loadClubRegistry().then(...)` — 탭 전환마다 새 리스너 추가
  - 변경 후: `renderClubManagement()` 등 UI 함수 직접 호출

### 기능 추가
- **클럽 목록 넘버링**: 클럽 관리 탭 목록에 클럽명 왼쪽에 순서 번호(`1.` `2.` ...) 표시

---

## v1.6.199 — 잔여 예산 미반영 경쟁조건 수정

- **현상**: 관리자 대시보드에서 클럽 관리 탭 진입 시 모든 클럽의 잔여 예산이 0으로 표시되는 경우 발생
- **원인**: `clubRegistry` (빠름)가 `globalHistory` (느림)보다 먼저 로드 완료되어 `renderClubManagement()` 호출 시 `lastHistoryList = []` 상태
- **수정**: `globalHistory` `.then()` 블록에서 `lastHistoryList` 설정 직후 `renderClubManagement()` 재호출 추가
  → `globalHistory` 로드 완료 시점에 클럽 관리 UI가 정확한 사용액으로 갱신됨

---

## v1.6.198 — 전사원 명부 카운트 이름 기반 폴백 완전 제거

- **현상**: 동명이인(이진호 4035 / 이진호 4277) 중 사번 없는 구형 정산 기록이 이름 폴백으로 잘못된 인물에 카운트 귀속
- **원인**: `_countAttendeesByEmpId` 내 이름 기반 폴백 로직 — 사번 없으면 이름으로 `directory` 검색 후 카운트
- **수정**: 사번(`employeeId`) 없는 참석자 기록은 카운트에서 **완전 제외**
  - 이름 폴백 코드 전면 삭제, 사번 기준 단일 경로만 유지
  - `idToName` 맵에 없는 사번도 무시 (명부 미등록 사원 오귀속 방지)

---

## v1.6.197 — 전사원 명부 카운트 0회 표시 수정

- **현상**: 정산 이력이 있음에도 전사원 명부의 "올해 누적 N회"가 0으로 표시
- **원인**: `recalculateDirectoryCountsFromGlobal()` 비동기 처리 완료 후 UI 갱신 누락 + `cur.count` 집계값 미동기화
- **수정**:
  - `_countAttendeesByEmpId`: `cur.counts[empId]` 업데이트 후 `cur.count`(집계합) 동기화 추가
  - `recalculateDirectoryCountsFromGlobal`: 처리 완료 후 `this.render()` 호출로 UI 즉시 반영
  - 진단용 콘솔 로그 추가: `[명부 카운트] YYYY년 정산 N건 처리, 명부 M명`

---

## v1.6.145 — 상품 카테고리 선택 안내 팝업 문구 다듬기

- 번역투 문장을 자연스러운 한국어로 수정
- "참석자 10명 이상 시에만 사용 가능합니다." → "참석자가 10명 이상일 때만 상품비를 사용할 수 있습니다."
- "한 해에 총 N원의 상품비 사용이 가능합니다." → "상품비는 한 해에 최대 N원까지 사용 가능합니다."
- (사용 중) "올해 최대 N원까지 상품비 사용 가능합니다." → "올해 남은 상품비 한도는 N원입니다."

---

## v1.6.144 — 상품 카테고리 선택 팝업 문구 2줄로 분리

- 상품 선택 시 안내(info) 팝업을 두 줄로 표시
  - 1줄: 참석자 10명 이상 조건 안내
  - 2줄: 연간 상품비 한도(`prizeLimit`, 기본 500,000원) 안내
- 이미 상품비 사용 중이면 2줄을 "올해 남은 한도" 안내로 전환

---

## v1.6.143 — 시설·장비 한도 관리자 설정 추가

- 관리자 정산 구간·비율 설정 패널에 "시설·장비 이사진 승인 시 법인카드 최대 한도" 입력란 추가
- `DefaultRules.facilityLimit = 85000` 기본값 추가 (관리자 수정 가능)
- 승인 팝업 금액은 `AppState.rules.facilityLimit`를 읽어 동적 표시

---

## v1.6.142 — 시설·장비 사전 승인 팝업 + 법인카드 한도 처리

- "시설 및 장비 이용료" 카테고리 선택 시 강조 경고 모달(⚠️ 주황 테마) 표시
- 안내: "사전 승인이 있어야 85,000원까지 법인카드 사용 가능합니다."
- ✅ 승인 완료 → 시설·장비(FACILITY) 유지, 법인카드 `facilityLimit`(기본 85,000원)까지
- ❌ 승인 없음 → 행사비(EVENT)로 자동 변경, 4구간 자부담 계산 적용
- `_facilityApproved` 플래그로 승인 상태 관리, 기존 시설비 항목 수정 시 자동 복원(재팝업 방지)

---

## v1.6.141 — APP_SPEC.md 전체 재작성

- 앱 전체 매뉴얼(`APP_SPEC.md`)을 `app.js` 실제 코드 기준으로 전면 재작성
- 항목별 계산방법 · 경우의 수 · 팝업/알림 문구 전체 망라 (17개 섹션)

---

## v1.6.120 — 금액 입력 시 법인/개인카드 토글 자동 설정

### 동작 방식
| 구간별 계산 결과 | 법인카드 토글 | 개인카드 토글 |
|---|---|---|
| 전액 법인 지원 (개인부담 0) | ✅ ON | ❌ OFF |
| 일부 자부담 발생 | ✅ ON | ✅ ON |
| 법인카드 0원 | ❌ OFF | ✅ ON |

- 금액 입력 또는 카테고리 변경 시 토글 자동 ON/OFF
- 법인카드 금액 자동 입력 후 수동 수정 가능
- 개인카드 = 총액 - 법인카드 (읽기 전용, 자동)
- 법인카드 토글 수동 ON 시: 토글 변경 없이 법인카드 금액만 재계산

---

## v1.6.119 — 브라우저 자동완성 선택 시 흰 배경 방지

- Microsoft Edge 등 브라우저 autofill 적용 시 입력 필드가 흰색으로 바뀌는 문제 수정
- `input:-webkit-autofill` CSS 오버라이드 적용 → 다크 테마 배경 및 글자색 유지

---

## v1.6.118 — 추가 차수 섹션 제거 + 비용 항목 자동 계산 통합

### 제거
- "추가 차수 임시 정산" HTML 섹션 완전 제거
- 관련 JS 함수 제거: `recalcExtraRounds`, `extraRoundBaseNum`, `relabelExtraRounds`, `addExtraRound`

### 추가 / 변경
- 비용 항목 추가 시 **금액 입력하면 구간별 계산식으로 법인카드 자동 입력** (수정 가능)
- 개인카드 = 총액 - 법인카드 자동 계산 (읽기 전용)
- 카테고리 변경 시에도 법인카드 재계산
- 법인카드 토글 ON → 법인카드 + 개인카드 금액 그룹 항상 표시
- 개인카드 영수증 업로드는 개인카드 토글 ON일 때만 표시

---

## v1.6.117 — 비용 항목 폼 토글 동작 개선

- 법인카드 토글만 ON → 총액 = 법인카드 자동 입력
- 법인카드 + 개인카드 모두 ON → 분배 모드 (법인카드 수동, 개인카드 자동)

---

## v1.6.116 — 차수별 법인카드 수동 입력 + 개인카드 자동 계산

- 각 추가 차수마다 법인카드 금액 직접 수정 가능 (기본값: 구간별 계산)
- 개인카드 = 총액 - 법인카드 자동 계산 (읽기 전용)

---

## v1.6.115 — 차수 번호 자동 매기기

- 비용 항목 N개 → 추가 차수는 (N+1)차부터 자동 라벨링

---

## v1.6.114 — 차수별 법인카드 구간별 계산

- 추가 차수 법인카드 금액을 참여 인원 기준 구간별 계산식으로 자동 계산 연동

---

## v1.6.113 — 추가 차수 임시 정산 (N차 동적 추가)

- "추가 차수 임시 정산" 섹션 추가 및 `+ 차수 추가` 버튼으로 N차까지 동적 추가 지원

---

## v1.6.112 — 2차 임시 정산 섹션 추가

- 1차 정산 외 추가 차수 입력을 위한 "2차 임시 정산" 간이 섹션 신규 구성

---

## v1.6.111 — 총 자부담 수정 팝업 메시지 개선

- 수동 수정 후 확인 팝업창에서 계산 전후의 차액 금액 계산 및 표기 가독성 개선

---

## v1.6.110 — 동명이인 사번 선택 중앙 오버레이 팝업 도입

- 다중 사번 소유 동명이인 사원 추가 시, 중앙 오버레이 팝업 창을 띄워 사번을 편리하게 선택하도록 UX 개편
- 단일 매칭 시 자동입력, 복수 매칭 시 팝업, 부분 매칭 시 인라인 검색결과 노출 지원

---

## v1.6.109 — bulkImportDirectory: JSON 이름 변경 시 구 항목 자동 정리

- `idToName` 역방향 매핑을 생성해 이름이 바뀐 사원(구 이름이 JSON에 없는 경우)의 기존 더미 항목을 로그인 시 검출 및 자동 정리

---

## v1.6.108 — employee_directory.json 내 이진호(PS) 데이터 정정

- 템플릿 명부 파일의 사번 4035의 이름을 "이진호(PS)" -> "이진호"로 수정하여 동명이인 자동 병합에 연동

---

## v1.6.107 — 이미 등록된 사번 복원 방지

- 로그인 시 `employee_directory.json` 복원 과정에서, 해당 사번이 이미 다른 이름으로 커스텀 등록되어 있는 경우 건너뛰도록 처리

---

## v1.6.106 — 전사원 명부 동명이인 별도 행 표시

- ids[]에 사번이 여러 개인 경우 각 사번을 독립된 행으로 렌더링하고, 카운트도 사원 인원수 기준으로 집계
- 삭제(×) 시 해당 사번만 리스트에서 제거

---

## v1.6.105 — 동명이인 이름 변경 시 ids[] 병합 처리

- 명부 등록 시 이미 존재하는 이름인 경우 덮어쓰지 않고 ids[]에 사번을 추가 병합

---

## v1.6.104 — 동명이인 사번 선택 드롭다운 추가

- 동일 이름의 복수 사번을 ids[]에 보관하고, 부분 매칭/정확 매칭/동명이인 상황에 맞춘 커스텀 드롭다운 `#attendee-name-dropdown` 구현

---

## v1.6.103 — Firebase 익명 인증 롤백

- 일부 환경의 인증 오류로 인해 익명인증 SDK 롤백 및 실시간 DB 접근 규칙을 기존과 같이 `true`/`true` 허용으로 복구

---

## v1.6.102 — Firebase 익명 인증 타이밍 재수정

- DB 클라이언트가 토큰을 동기화하기 전 접근 오류를 차단하기 위해 `onAuthStateChanged` 수신 후 Promise resolve 처리

---

## v1.6.101 — Firebase 익명 인증 타이밍 버그 수정

- 인증 완료 전에 데이터를 미리 로드하려던 레이스 컨디션을 해결하고자 `firebaseAuthReady` 대기 구조 적용

---

## v1.6.100 — Firebase 익명 인증 추가 및 DB 규칙 보안 강화

- `firebase-auth-compat.js` SDK 추가, `signInAnonymously()` 인증 및 DB 읽기/쓰기 조건에 `auth != null` 검증 적용

---

## v1.6.99 — 계산식 보호 및 자동 검증 레이어 추가

- `SettlementValidator` 도입으로 calculate 결과(비용 합계, 인당 자부담 등 전 항목)를 독립 수식과 교차 검증 (허용오차 ±1원)
- 계산식 연동 영역 Object.freeze 런타임 변조 차단 및 실패 시 화면 경고 배너 렌더링
- app.js 내 `// ⛔ CALCULATION_LOCKED` 마커 도입

---

## v1.6.98 — 번역 누락 추가 수정

- '배정 합계 / 미배정' 영역 번역 및 '✔️ 저장되었습니다' 팝업 다국어 지원 보완

---

## v1.6.97 — 동적 렌더 영역 번역 적용

- 클럽 관리(배정/잔여 예산, 추가 배정 등), 이력 카드(정산인, 총소요 등)의 동적 영역 전체에 `t()` 동적 바인딩 적용

---

## v1.6.96 — 로그인 상태 배지 번역 덮어쓰기 버그 수정

- 로그인 상태 배지 다국어 전환 시 오프라인 배지로 회귀하는 버그 해결

---

## v1.6.95 — 번역 누락 전면 수정

- UI 텍스트(수정 모드 배너, 법인/개인카드, 영수증, 빈 상태 안내 등)에 `data-i18n` 속성 추가
- alert/confirm 25건을 `t()`로 전면 대체 및 신규 번역 키 50여 개 추가

---

## v1.6.83 — UI 정리 / 실시간 클럽 동기화 기반 구축

### 변경 내용
- "현재 정산 초기화" 버튼 라벨 → "정산 초기화"로 단축.
- 정산 이력 탭의 "모두 삭제" 버튼 제거 (관리자 탭에서 개별 삭제하는 방식으로 통일).
- 언어 전환 UI를 `<button>` 토글 → `<select>` 드롭다운으로 변경.
- `window._onClubRegistryUpdate` 전역 콜백 도입:
  `loadClubRegistry()`의 `.on('value')` 리스너가 갱신될 때마다
  `renderClubOptions()`, `renderClubManagement()`, `renderClubHistorySelect()`를 일괄 호출해
  관리자·유저 화면 클럽 목록이 실시간으로 동기화되도록 기반 마련.

---

## v1.6.84 — 숫자 입력 불가 긴급 수정

### 원인
- v1.6.83에서 `#clear-all-btn` 버튼을 HTML에서 제거했는데,
  `app.js`의 `DOMContentLoaded` 핸들러에서 해당 요소에 `.addEventListener()`를 호출하고 있었음.
- `null.addEventListener()` → `TypeError` 발생 → 이후 모든 이벤트 리스너(PIN 키패드, 금액 입력 등) 등록 실패.

### 수정
- `app.js`에서 `clear-all-btn` 이벤트 리스너 코드 완전 제거.

---

## v1.6.85 — 한국어/영어 전체 번역 지원

### 어떻게?
- `lib/i18n.js` 전면 재작성: `TRANSLATIONS.ko` / `TRANSLATIONS.en` 약 150개 키 정의.
  - 커버 범위: 헤더, 탭, 로그인/회원가입 모달, 정산 탭, 설정 패널, 참석자 탭, 명부 탭,
    이력 탭, 클럽별 이력 탭, 차트, 관리자 화면, 이메일·피드백·팝업·엑셀저장·초기화 모달 전체.
- `index.html` 전체 정적 요소에 `data-i18n` / `data-i18n-ph` / `data-i18n-title` 속성 부여.
- `applyTranslations()` 함수에 특수 처리 추가:
  - 탭 버튼의 동적 카운트 `<span>`을 innerHTML 재구성 방식으로 보존.
  - 카테고리 `<select>` 옵션, 클럽/관리자 선택 첫 번째 옵션 텍스트 갱신.
- 로그인 페이지에 `#login-lang-select` 드롭다운 추가.
- 동적으로 렌더링되는 텍스트(빈 상태 안내, 이력 카드 레이블 등)는 `t(key)` 함수로 출력.

---

## v1.6.86 — 언어 선택: 로그인 페이지 전용 / 전체 UI 즉시 갱신

### 변경 내용
- 헤더에 있던 `#lang-select` 드롭다운 제거
  (로그인 이후 화면에서는 언어 전환 불필요, full-width 표시 버그도 해소).
- 언어 변경 시 전체 UI 즉시 갱신 흐름 확립:
  `setLang()` → `AppState.render()` → `applyTranslations()`
  — `render()` 끝에서 `applyTranslations()`를 항상 호출해 동적 콘텐츠까지 반영.

---

## v1.6.87 — 클럽명 실시간 동기화 수정 + 삭제 팝업

### 문제
- 유저가 `register-new-club-btn`으로 클럽을 직접 등록할 때
  `AppState.clubId`가 저장되지 않아, 이후 관리자가 클럽명을 수정해도 유저 화면에 반영되지 않았음.
- `renderClubOptions()`가 레지스트리에 없는 구 클럽명을 phantom option으로 추가해
  삭제된 클럽이 드롭다운에 계속 표시되는 문제.

### 수정
1. **`register-new-club-btn` 핸들러**: 신규 클럽 생성 시 `AppState.clubId = newClubId` 저장.
   동일 이름 클럽이 이미 레지스트리에 있으면 기존 `clubId`를 재사용.
2. **`loadClubRegistry()` `.on('value')` 콜백**:
   - `clubId`가 있고 레지스트리에 존재 → 이름 변경 감지 시 `AppState.clubName` 즉시 갱신.
   - `clubId`가 있는데 레지스트리에 없음(삭제) → `clubName` / `clubId` 초기화 후 팝업 표시.
3. **`renderClubOptions()`**: phantom option 로직 제거. 레지스트리 기준으로만 렌더링.
   `clubId` 기준으로 현재 선택 클럽명 갱신, 없으면 초기화 + 팝업.
4. **`#club-not-found-modal`** 추가 (`index.html`):
   "기존 클럽이 삭제되었습니다. 클럽을 새로 선택해 주세요." 안내 후 확인 버튼으로 닫기.
5. `window.showClubNotFoundModal` 전역 등록 — `AppState` 내부에서도 호출 가능.

---

## v1.6.88 — 관리자 클럽명 변경 시 기존 정산 이력 소급 갱신

### 문제
- 관리자가 클럽명을 수정해도 과거 정산 이력의 `clubName` 필드는 변경 전 이름 그대로 남아,
  관리자 "클럽별 정산이력" 탭에서 구 이름으로 표시되었음.

### 수정
1. **`newHistoryItem`에 `clubId` 필드 추가** (`finalizeSettlement`):
   신규 정산부터 `clubId`가 이력에 저장되어 이름 변경 추적이 가능해짐.

2. **`AppState.renameClubInHistory(clubId, oldName, newName)` 메서드 추가**:
   - `globalHistory` 전체 스캔 → `clubId` 일치 또는 (기존 이력이라 `clubId` 없는 경우) `clubName` 일치 항목의 `clubName`을 새 이름으로 일괄 갱신.
   - `settlements/모든PIN/settlementHistory` 전체 스캔 → 동일 조건으로 개인 이력도 갱신.
   - Firebase 다중 경로 업데이트(`ref().update(updates)`)로 단일 트랜잭션 처리.

3. **관리자 클럽 폼 submit 핸들러**:
   `addOrUpdateClub()` 호출 전 구 이름을 저장, 이름이 실제로 변경된 경우에만 `renameClubInHistory()` 실행.

4. **`loadClubRegistry()` 콜백에서 메모리 이력 즉시 반영**:
   현재 로그인 중인 유저는 Firebase 갱신을 기다리지 않고 `AppState.settlementHistory` 배열을 즉시 수정해 화면에 바로 새 이름이 표시됨.

---

## v1.6.89 — 클럽 예산 실시간 동기화

### 변경 내용
- `loadClubRegistry()` `.on('value')` 콜백에서 클럽 `budget` 변경을 감지해
  `AppState.annualBudget`을 즉시 갱신.
- 관리자가 예산을 수정하는 순간 유저 화면에도 실시간 반영됨.

---

## v1.6.90 — 관리자 규칙 실시간 동기화 + 정산 이력 수정 모드

### 관리자 규칙 실시간 동기화
- `AppState.loadGlobalSettings()` 추가: `globalSettings/rules` 노드를 `.on('value')`로 구독.
  관리자가 자부담 구간·비율을 변경하면 모든 유저 화면에 즉시 반영됨.
- `AppState.saveGlobalRules()` 추가: `updateRules()` / `resetRules()` 호출 시 Firebase에 저장.
- `window._onGlobalSettingsUpdate` 전역 콜백 → `setSettingsFormValues()` 재호출.

### 정산 이력 수정 모드
- 정산 이력 카드에 **"✏️ 수정"** 버튼 추가.
- 버튼 클릭 시 `AppState.loadHistoryEntryForEdit(entry)` 호출:
  비용 항목·참석자·클럽명이 정산 탭으로 복원되고 **수정 모드 배너** 표시.
- 수정 후 **"엑셀로 저장"** 클릭 → `AppState.editingHistoryId`가 있으면
  신규 확정 대신 `updateHistoryEntry()` 실행해 기존 이력 덮어씀.
- 수정된 항목에 **"수정됨"** 오렌지 뱃지 표시 (`isEdited: true` 플래그).
- 관리자 "클럽별 정산이력"에도 수정 버튼·뱃지 동일 적용.

---

## v1.6.91 — 클럽 삭제 팝업 오탐 수정

### 문제
1. 클럽명 **변경** 시 (`clubId` 미설정 상태)에도 "삭제됨" 팝업이 표시되었음.
2. 초기 로그인 직후 레지스트리 첫 로드 시에도 팝업이 뜰 수 있었음.
3. 클럽 선택 변경 시 이전 클럽의 `usedBudget`이 남아 예산 계산 오류 발생.

### 수정
- `renderClubOptions()`에서 팝업 호출 완전 제거.
  삭제 팝업은 `loadClubRegistry()` 콜백에서만 발생(확인된 삭제 시).
- `loadClubRegistry()`에 `initialLoad` 플래그 도입:
  첫 번째 `on('value')` 콜백에서는 팝업 억제.
- 클럽 변경 핸들러에서 `AppState.usedBudget = 0` 리셋 추가.

---

## v1.6.92 — 예산 표시 항상 관리자 레지스트리 기준

### 문제
- `annualBudget` / `usedBudget`이 유저별 localStorage에 캐시되어
  관리자가 설정한 값과 다르게 표시되었음.

### 수정
- `AppState.getClubBudget()` 추가:
  `clubId → clubName → annualBudget 캐시` 3단계 폴백으로 항상 레지스트리 값 반환.
- `AppState.getClubUsedBudget()` 추가:
  `priorUsed(관리자 설정) + 올해 정산 이력 합산`으로 실사용액 계산.
- 모든 예산 표시 위치(`render()`, `setSettingsFormValues()`, `updateRemainingDisplay()`)를
  새 메서드로 교체.
- `setting-annual-budget` 인풋을 `readonly`로 변경.

---

## v1.6.93 — 예산 표시 수정 (type=number 콤마 호환)

### 문제
- `setting-annual-budget` 인풋이 `type="number"`인데
  `formatAmount()` 포맷된 문자열("800,000")을 대입하면 number 입력이 콤마를 거부해 0으로 표시됨.
- 일부 클럽만 예산이 정상 표시되는 현상 원인.

### 수정
- `index.html`: `setting-annual-budget` 인풋을 `type="number"` → `type="text"`로 변경.
- `app.js`: `getClubBudget()` 3단계 폴백 명확화 (clubId → 이름 → annualBudget 캐시).
- `getClubUsedBudget()` 로직 리팩터링(가독성 개선, null 체크 강화).

---

## v1.6.94 — 코드 버그 3종 수정 + 언어 선택 UI 개선

### 크리티컬 버그 수정

**1. `updateHistoryEntry` Firebase 경로 오류 (P0)**
- Firebase는 JS 배열을 `{0: {...}, 1: {...}}` 객체로 저장하므로,
  배열 인덱스(`/settlementHistory/0`)로 접근하면 실제 키와 불일치 → 업데이트 실패.
- **수정**: 개인 `settlementHistory` 저장 시 배열 전체를 `set()`으로 덮어쓰는 방식으로 변경.
  네트워크 실패 시 사용자에게 경고 alert 추가.

**2. `recalculateDirectoryCounts` 중복 카운트 (P0)**
- 수정 이력(`isEdited: true`)과 원본 이력이 동일 `id`라도 각각 카운트되어 참석 횟수가 2배가 될 수 있었음.
- **수정**: `seenIds` Set으로 동일 `id` 이력은 1회만 집계.

**3. `loadHistoryEntryForEdit` 구버전 데이터 호환 (P1)**
- 구버전 이력(clubId 없음)을 수정 모드로 열 때 `clubId`가 빈 채로 진입.
- `entry.memberCount` 누락 시 참석자 수 0으로 표시.
- **수정**: entry 유효성 검사 추가, `memberCount` → `attendees.length` 폴백,
  `clubId` 없으면 이름으로 레지스트리에서 역조회해 복원.

### UI 개선
- 로그인 페이지 언어 선택: `<select>` 드롭다운 → pill 토글 버튼으로 변경.
  선택된 언어는 흰 배경·밝은 색으로 강조, 미선택은 반투명 처리.

---

# 변경 사항 정리 (v1.4.1 → v1.5.2)

## 1. 비용 항목 추가 폼 재설계 (법인카드 / 개인카드)

### 왜?
- 기존에는 결제수단을 `<select>`(법인카드/개인카드/분할)로 선택하는 방식이라 분할결제 시 입력이 직관적이지 않았음.
- "한 항목을 법인카드+개인카드로 나눠 결제"하는 경우가 많아, 각 카드별 금액과 영수증을 따로 입력할 수 있어야 했음.

### 어떻게?
- 카드 종류 선택을 `<select>` → **체크박스 2개**(법인카드/개인카드)로 변경.
  - 둘 다 체크 해제는 불가능(`updateCardTypeUI()`에서 자동으로 법인카드 재체크).
  - 법인카드만 체크 → 법인카드 금액/영수증 입력칸만 표시.
  - 개인카드만 체크 → 개인카드 금액/영수증 입력칸만 표시.
  - 둘 다 체크(분할결제) → 두 입력칸 모두 표시 + **개인카드 금액 = 총금액 - 법인카드 금액** 자동 계산.
- 체크박스를 카드 헤더 우측에 배치하고, 토글 스위치 형태(`.card-toggle`)로 스타일링.
- 라벨을 "법인카드 결제 금액 (원)" → "법인카드"로 단순화하고, 영수증 첨부 버튼을 같은 줄에 배치(파일 input은 숨기고 `<label class="btn-secondary">영수증</label>`로 트리거).
- 엑셀 내보내기는 분할결제여도 **항목당 1줄**로 정리되는 기존 로직 유지(`addExpense()`에서 `cardType: 'split'/'corporate'/'personal'`로 구분해 저장).

### 관련 함수 (app.js)
- `addExpense()`, `startEditExpense()`, `cancelEdit()`, `updateCardTypeUI()`

---

## 2. 전사원 명부 - EMP ID 정규화 (4자리 숫자)

### 왜?
- 기존 `lib/employee_directory.json`에는 `CG000424`, `KO003098`, `01147900` 등 형식이 제각각이었음.
- 실제 엑셀(클럽비용정산 양식.xlsx)의 "Global ID" 시트 기준, **EMP ID는 전부 4자리 숫자**(예: `0424`, `4121`)이어야 함.

### 어떻게?
- 업로드된 엑셀의 "Global ID" 시트(B열=EMP ID, C열=이름)를 파싱해서 이름→EMP ID(4자리, zero-pad) 매핑 생성.
- `lib/employee_directory.json`을 이 매핑 기준으로 재구성 (1315명 → 중복 제거 후 1272명, 이후 1273명).
- 시트에 없는 일부 인원(`이진호(PS)` 등)은 Global ID 시트의 동일인 항목(`이진호(2)`, EMP ID 4035)으로 매칭해 수동 보정.

### ⚠️ 기존 사용자 데이터(로컬/Firebase) 마이그레이션
- 문제: 사용자가 이미 PIN 로그인해서 사용 중이면, 본인 localStorage/Firebase에 **예전 ID(CG00xxxx, 8자리 Global ID 등)가 그대로 저장**되어 있어서 새 `employee_directory.json`을 덮어써도 반영이 안 됨.
- 해결: `bulkImportDirectory()`에 **마이그레이션 로직 추가** — 이름이 일치하는데 기존 ID가 `4자리 숫자` 형식이 아니면, 새 EMP ID로 자동 갱신.
  ```js
  } else if (typeof entry === 'object' && entry.id !== employeeId && !/^\d{4}$/.test(String(entry.id))) {
      entry.id = employeeId;
      updated++;
  }
  ```
- 이 로직은 **앱 로드 시 / PIN 로그인 시 매번 자동 실행**되므로, 사용자가 새로고침하면 자동으로 최신 EMP ID로 갱신됨.

---

## 3. 전사원 등록 폼 - EMP ID/이름 라벨 및 안내

### 왜?
- "사번"이라는 용어보다 "EMP ID"가 실제 엑셀 컬럼명과 일치.
- 신규 등록 시 동명이인 여부를 미리 확인하지 못해 중복/오기입 위험.

### 어떻게?
- "사번" 라벨 → "EMP ID"로 변경, placeholder도 `CG001234` → `1234`로 변경.
- 이름 입력 시, 동일 이름이 명부에 이미 있으면 **"등록된 EMP ID: XXXX"** 힌트를 바로 아래 표시.
- EMP ID까지 입력했을 때:
  - 이름+EMP ID가 **완전히 동일** → 제출 시 에러 팝업("이미 동일한 이름과 EMP ID로 등록되어 있습니다").
  - 이름은 같은데 EMP ID가 **다름** → 실시간으로 빨간 경고 문구 표시: **"동일한 이름이 존재합니다. 이름뒤에 (부서명)을 적어주세요."** (동명이인 구분 유도)

### 관련 함수 (app.js)
- `updateDirNameHint()`, `trySubmitDir()`, `getExistingId()`

---

## 4. 전사원 명부 삭제 권한 제한

### 왜?
- 명부 데이터(1300명 가까운 인원)를 아무나 실수로 삭제하면 복구가 어려움.

### 어떻게?
- 명부 추가(등록)는 **누구나 가능**.
- 명부 삭제(× 버튼, `deleteFromDirectory()`)는 **관리자(PIN 000000) 또는 개발자(PIN 002531/김종필)만 가능**하도록 제한.
  - 권한 없는 사용자에게는 × 버튼 자체가 렌더링되지 않음.
  - 함수 내부에서도 한 번 더 권한 체크 후 `alert()`로 안내.

---

## 5. 버전 관리 (sw.js)

매번 `index.html` / `app.js` / `style.css` 수정 후:
1. (APK 빌드 안 함 — `app/src/main/assets/` 동기화 단계는 더 이상 수행하지 않음, 웹 버전만 운영)
2. `sw.js`의 `APP_VERSION` 1단계씩 증가 → 사용자에게 "업데이트 있음" 알림 자동 표시
3. git commit & push

현재 버전: **1.6.51**

---

## 6. 금액 입력란 1,000단위 콤마(,) 자동 표시 (v1.6.50)

### 어떻게?
- `app.js` 최상단(모듈 스코프, `updatePerPersonSelfPayIcon` 근처)에 헬퍼 3종 추가:
  - `formatAmount(num)`: `Number(num||0).toLocaleString('ko-KR')` — 화면 표시용 콤마 포맷.
  - `parseAmount(val)`: 문자열에서 숫자 외 문자 제거 후 `parseInt` — 입력값 읽기용.
  - `setupCurrencyInput(el)`: `input` 이벤트마다 콤마를 다시 적용(커서 위치 보정 포함).
- 대상 입력란(모두 `type="number"` → `type="text" inputmode="numeric"`로 변경):
  `prev-prize-input`, `expense-amount-input`, `expense-corporate-amount-input`,
  `expense-personal-amount-input`, `setting-used-budget`, `result-total-self-pay-input`,
  `admin-setting-limit1/2/3`, `admin-setting-deduction4`, `club-total-budget-input`, `club-budget-form-input`.
- 초기화(`DOMContentLoaded`) 시점에 위 ID 배열을 순회하며 `setupCurrencyInput()` 일괄 적용.
- 기존에 `parseInt(x.value, 10)`로 읽던 부분은 모두 `parseAmount(x.value)`로 교체.
- 화면에 값을 세팅(`.value = ...`)하던 부분은 모두 `formatAmount(...)`로 교체(빈 문자열 리셋은 그대로 `''` 유지).

### 새 입력란을 추가할 때
- 금액 입력란이면 `type="text" inputmode="numeric"`로 만들고, 위 배열에 ID를 추가하거나 직접 `setupCurrencyInput(el)` 호출.
- 값을 읽을 때는 `parseAmount()`, 화면에 쓸 때는 `formatAmount()`를 사용해야 콤마가 깨지지 않음.

---

## 7. 클럽 "이전 사용 금액" 입력 위치 변경 (v1.6.50)

### 왜?
- 관리자가 클럽별로 "이전 사용 금액"을 입력하던 방식은, 실제로는 각 사용자(클럽 담당자)가 자신의
  누적 사용 금액을 더 잘 알고 있어 입력 주체를 바꾸는 것이 자연스러움.

### 어떻게?
- 관리자 클럽 관리 폼(`#club-form`)에서 `#club-prior-used-form-input` 입력란 삭제.
  대신 안내 문구로 "사용자가 ⚙️ 설정의 '기존에 사용한 누적 금액'에서 직접 입력"하도록 안내.
- `AppState.addOrUpdateClub(clubId, name, budget, priorUsed)`의 `priorUsed`는 그대로 유지하되,
  관리자 폼 제출 시 기존 클럽의 `priorUsed` 값을 그대로 보존(편집해도 변경되지 않음, 신규 클럽은 0).
- 기존 `AppState.usedBudget` ↔ `club.priorUsed` 동기화 로직(`syncBudgetFromClub`, v1.6.42)은 그대로 유지됨
  → ⚠️ 이 부분은 [CALCULATION_SPEC.md](CALCULATION_SPEC.md)와 무관하므로 자유롭게 조정 가능.

---

## 8. 클럽별 "추가 배정" 버튼 (v1.6.50)

### 어떻게?
- `index.html`에 새 모달 `#add-club-budget-modal` 추가 (입력란 `#add-club-budget-input`,
  버튼 `#add-club-budget-confirm-btn` / `#add-club-budget-cancel-btn`).
- `app.js`에 `openAddClubBudgetModal(clubName, onConfirm)` 모듈 함수 추가
  (`showDiffPopup` 함수 바로 위에 위치) — 모달을 열고 확인 시 `onConfirm(amount)` 콜백 호출.
- `renderClubManagement()`의 각 클럽 행에 "추가 배정" 버튼(`.btn-add-club-budget`) 추가.
  클릭 시 입력한 금액을 `club.budget`에 더해 `AppState.addOrUpdateClub()`으로 저장 후 재렌더링.

---

## 9. 헤더 디자인 변경 (v1.6.51)

- 부제목(`<p class="subtitle">엑셀 정산 양식의 핵심 계산...</p>`) 삭제.
- 로고 아이콘(📊) → 빨간색 굵은 글씨 "3M" 텍스트로 변경.
- 제목을 "클럽 비용 정산기" → "클럽 비용 정산"으로 줄여 한 줄에 표시되도록 수정
  (`logo-area`에 `flex-wrap:nowrap`, `<h1>`에 `white-space:nowrap` 적용).
  - `app.js`의 `setAdminMode()`에서 멤버 모드 제목도 동일하게 "클럽 비용 정산"으로 동기화.
- 우측 상단 "📩 요청사항" 버튼 → "📝 요청"으로 텍스트 단축 + 테마 색상(그라데이션 pill, `#feedback-list-open-btn`과 동일한 스타일 톤)으로 재디자인.
