# 클럽 비용 정산 앱 수정사항

**버전 범위:** v1.6.141 ~ v1.6.145  
**날짜:** 2026-06-25 ~ 06-26

---

## v1.6.141 — APP_SPEC.md 전체 재작성

- 앱 전체 매뉴얼(`APP_SPEC.md`)을 `app.js` 실제 코드 기준으로 전면 재작성
- 항목별 **계산방법 · 경우의 수 · 팝업/알림 문구**를 빠짐없이 망라
- 17개 섹션 구성: 데이터 원칙, 비용 카테고리, 자부담 계산(LOCKED), 상품비 로직,
  카드 유형, 클럽 잔여 예산, 정산 결과, 자부담 수동 수정, 정산 확정 흐름,
  이력 수정 모드, 엑셀 셀 매핑, 전사원 명부, 권한 모델, 전체 팝업/알림 목록,
  차트 탭, PWA/캐시, i18n

---

## v1.6.142 — 시설·장비 사전 승인 팝업 + 법인카드 한도 처리

### 동작
- 비용 항목 추가 시 **"시설 및 장비 이용료"** 카테고리를 선택하면 강조 경고 모달 표시
  (⚠️ 아이콘 + 주황색 테마, `facility-approval-modal`)
- 안내 문구: "사전 승인이 있어야 **85,000원**까지 법인카드 사용 가능합니다."
  / "승인 없이 사용 시 행사비 계산 방식(자부담)이 적용됩니다."

### 분기
| 사용자 선택 | 카테고리 | 법인카드 한도 |
|---|---|---|
| ✅ 승인 완료 | 시설·장비(FACILITY) 유지 | `facilityLimit`(기본 85,000원)까지 |
| ❌ 승인 없음 | 행사비(EVENT)로 자동 변경 | 4구간 자부담 계산 적용 |

- `_facilityApproved` 플래그로 승인 상태 관리 (승인 완료 버튼을 눌러야 `true`)
- 승인 완료 시 `_calcCorpForItem`에서 `Math.min(amount, facilityLimit)`로 법인카드 산정
- 기존 시설비 항목 **수정** 시 승인 플래그를 자동 복원해 재팝업 방지
  (`startEditExpense`에서 `_facilityApproved = (item.category === FACILITY)`)

---

## v1.6.143 — 시설·장비 한도 관리자 설정 추가

- 관리자 **정산 구간·비율 설정** 패널에
  "🏢 시설·장비 이용료 이사진 승인 시 법인카드 최대 한도 (원)" 입력란 추가
- `DefaultRules.facilityLimit = 85000` 기본값 추가 — **관리자가 수정 가능**
- 저장 시 `AppState.updateRules({ ..., facilityLimit })`로 반영
- 승인 팝업의 금액(`#facility-modal-limit`)은 `AppState.rules.facilityLimit`를
  읽어 **동적으로 표시** (관리자가 한도를 바꾸면 팝업 문구도 자동 반영)

---

## v1.6.144 — 상품 카테고리 선택 팝업 문구 2줄로 분리

- 상품 카테고리 선택 시 안내(info) 팝업을 **두 줄**로 표시
  - 1줄: 참석자 10명 이상 조건 안내
  - 2줄: 연간 상품비 한도(`prizeLimit`, 기본 500,000원) 안내
- 이미 상품비를 사용 중이면 2줄을 "올해 남은 한도" 안내로 전환

---

## v1.6.145 — 상품 카테고리 선택 안내 팝업 문구 다듬기

번역투 문장을 자연스러운 한국어로 수정 (`app.js` `infoMsg`):

| 구분 | 이전 | 수정 후 |
|---|---|---|
| 1줄 | 참석자 10명 이상 시에만 사용 가능합니다. | 참석자가 10명 이상일 때만 상품비를 사용할 수 있습니다. |
| 2줄 (첫 사용) | 한 해에 총 500,000원의 상품비 사용이 가능합니다. | 상품비는 한 해에 최대 500,000원까지 사용 가능합니다. |
| 2줄 (사용 중) | 올해 최대 N원까지 상품비 사용 가능합니다. | 올해 남은 상품비 한도는 N원입니다. |

---

## 참고 — 현재 `DefaultRules` 기준값

| 키 | 값 | 설명 |
|---|---|---|
| `limit1` | 30,000 | 가구간 한도 (자부담 0%) |
| `limit2` | 60,000 | 나구간 한도 |
| `rate2` | 0.2 | 나구간 자부담 비율 |
| `limit3` | 120,000 | 다구간 한도 |
| `rate3` | 0.4 | 다구간 자부담 비율 |
| `deduction4` | 85,000 | 라구간 자부담 공제액 |
| `prizeLimit` | 500,000 | 상품비 연간 최대 사용 금액 |
| `facilityLimit` | 85,000 | 시설·장비 이사진 승인 시 법인카드 최대 한도 |

> 위 값은 모두 관리자(PIN: 000000) 설정으로 변경 가능하며, 변경 시 Firebase
> `globalSettings/rules` 기준으로 모든 사용자 화면에 반영됨.
