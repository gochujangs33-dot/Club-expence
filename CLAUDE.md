# 작업 가이드 (Antigravity 에이전트 필독)

이 문서는 "클럽 비용 정산기" 웹앱 작업 시 **반드시 지켜야 할 규칙**을 정리한 것입니다.
새 작업을 시작하기 전에 이 문서를 먼저 읽으세요.

---

## 0. 데이터 원칙 (최우선 — 어떤 작업에도 예외 없음)

**모든 메인 데이터의 기준은 관리자(PIN: 000000) 설정값이다.**

- 총 금액, 누적금액, 상품비 금액, 잔여 예산, 정산 구간·비율, 클럽명, 회원 관리, 전사원 명부 —
  이 모든 항목은 관리자가 설정한 Firebase 공유 경로 기준으로 유저 화면에 표시된다.
- 유저 개인 Firebase(`settlements/{PIN}`) 또는 localStorage에 위 항목을 저장하거나 읽는 코드는
  **절대 추가하지 않는다.** 기존에 그런 코드가 있다면 발견 즉시 제거한다.
- 관리자 기준 Firebase 경로: `globalSettings/rules`, `clubRegistry/`, `globalHistory/`, `deletedHistoryIds/`
- 참고 문서: [`APP_SPEC.md`](APP_SPEC.md) — 항목별 계산방법·경우의수·팝업 문구 전체 명세

---

## 1. 절대 규칙 (Hard Constraints)

0. **모든 계산/숫자 표시 관련 버그는 무관용(zero-tolerance) 원칙**
   - 정산 금액은 실제 회사 비용 처리와 직결됨 — "결과값은 맞는데 안내 문구/팝업/표시만 틀린" 경우도
     동일한 심각도로 취급할 것 (예: v1.6.76 자부담 차액 팝업 버그).
   - 계산 관련 코드를 수정하거나 새로 만들 때는 **CALCULATION_SPEC.md / 계산식_명세서.md의 예시값으로
     반드시 직접 대조 검증**.
   - 이벤트 중복 호출, 상태값(`AppState.xxx`) 갱신 순서 등 **부수효과가 있는 로직은 특히 꼼꼼히 점검**
     — "수정 전 값" 같은 기준점은 매번 변하는 상태값이 아니라 고정된 자동계산값을 사용해야 함.
   - 화면에 보이는 모든 금액/비율/카운트(정산 결과, 엑셀, 차트, 명부 카운트, 잔여 예산 등)는
     "어떤 변수에서 왔는지"를 코드로 직접 확인하기 전까지 맞다고 가정하지 말 것.

1. **모든 답변/커밋 메시지 설명은 한국어로 작성** (코드 식별자/주석은 기존 스타일 유지).
2. **계산 로직 & 엑셀 매핑은 절대 임의 변경 금지** ⛔
   - 기준 문서: [`CALCULATION_SPEC.md`](CALCULATION_SPEC.md), [`계산식_명세서.md`](계산식_명세서.md)
   - **코드에서 `// ⛔ CALCULATION_LOCKED` ~ `// ⛔ CALCULATION_LOCKED END` 사이의 모든 코드는**
     **사용자의 명시적 서면 요청 없이는 1행도 수정·삭제·재작성 불가**.
   - 보호 대상 함수 목록 (app.js):
     - `SettlementValidator._selfPay` — 4구간 자부담 독립 재계산
     - `SettlementValidator.validate` — 계산 결과 교차 검증
     - `SettlementCalculator.calculate` — 핵심 정산 계산
     - `SettlementCalculator.calculateSelfPayPerPerson` — 인당 자부담 4구간
     - `generateExcelFile` 내 `setCellValue` 셀 매핑(K4~K30, L12~L25 등)
   - `SettlementValidator`가 검증 실패(`_validation.valid === false`)를 반환하면
     **커밋 전에 반드시 원인을 찾아 수정**해야 하며, 경고를 무시하고 배포하지 않음.
   - 템플릿 셀 좌표가 바뀌는 경우에도 계산식 자체는 그대로 두고 `setCellValue` 의 ref만 수정.
3. **웹 전용 배포 — APK 자산 동기화 금지**
   - `app/src/main/assets/` 디렉터리에 변경 파일을 복사/동기화하지 않음 (과거에는 했었지만 현재는 GitHub Pages 웹앱만 운영).
4. **권한 모델 변경 시 [`PERMISSIONS.md`](PERMISSIONS.md) 갱신**
   - PIN `000000` = 관리자, `002531` = 개발자(김종필, 권한상 일반 사용자 + 명부 삭제권만 추가), 그 외 = 일반 회원.
5. **데이터 동기화 기능은 신중하게**
   - 계정(PIN)별 `directory`(전사원 명부 누적 카운트)는 **계정 독립 저장**이며, 계정 간 공유/병합 시도(v1.6.69~73)는
     카운트 초기화 사고를 일으켜 **전면 롤백(v1.6.74)** 됨. 유사한 "공유/병합" 기능은 다시 시도하기 전
     반드시 사용자와 설계를 재확인할 것.
   - 사용자가 회귀(데이터 초기화 등)를 보고하면 **추가 패치보다 즉시 직전 안정 버전으로 롤백**을 우선.
6. **버그 수정 코드 — 재수정·롤백 금지** ⛔ (아래 4개 항목은 각각 재발 버그를 잡은 fix임)

   | 함수 / 위치 | 핵심 불변량 | 수정 버전 | 재수정 시 재발하는 버그 |
   |---|---|---|---|
   | `_countAttendeesByEmpId` | 사번(`employeeId`) 없는 참석자는 **무조건 스킵** — 이름 기반 폴백 코드 없음 | v1.6.198 | 동명이인 카운트 오귀속 (이진호 4277↔4035 혼용) |
   | `recalculateDirectoryCountsFromGlobal` | 비동기 처리 완료 후 `if (this.isLoggedIn) this.render()` 호출 필수 + `cur.count` 집계합 동기화 | v1.6.197 | 전사원 명부 카운트 0회 표시 |
   | `loadClubRegistry` | `.on('value', ...)` 등록 **직전** 반드시 `.off('value')` 호출 | v1.6.200 | 탭 전환마다 리스너 누적 → 클럽 중복 생성 |
   | `renderAdminDashboard` step 3 | `AppState.loadClubRegistry().then(...)` **재호출 금지** — UI 함수 직접 호출, `renderAllCharts`는 호출하지 않음(rAF+`.then()` 담당) | v1.6.200~201 | 리스너 누적 / 차트 3중 렌더 깜빡임 |
   | `renderClubManagement` | Firebase 동기화 쓰기는 `historyLoaded === true`일 때만 (`needsUpdate && firebaseDb && historyLoaded`) | v1.6.201 | 이력 로드 전 0원이 공유 clubRegistry를 덮어씀 → 전 접속자 잔여예산 오표시 |
   | `renderAdminDashboard` globalHistory `.then()` | `historyLoaded = true` 세팅 + `renderClubManagement()`·`updateChartsBudgetStats()` 호출 유지 | v1.6.199~201 | 잔여예산·차트 통계 타일 0원 표시 |
   | `updateHistoryEntry` | 이력 금액 수정 시 `clubRegistry.usedBudget`/`.prizeUsed`를 **차액(delta)만큼만** 보정 (전체 재계산 아님, 새 값 통째로 더하기 아님) | v1.6.213 | 이력 수정 시 예산이 새 금액만큼 추가된 것처럼 잘못 반영 |
   | `getClubUsedBudget` | `editingHistoryId`가 설정된 동안 이 이력 자신의 기존 지원금(`_editingOriginalSupport`)을 "이미 사용한 금액"에서 반드시 제외 | v1.6.214 | 이력 수정 화면에서 자기 자신과 비교돼 잔여 예산 초과 오탐 |
   | 자부담 최종값 산출 전체 (`render`/`finalizeSettlement`/`downloadExcelOnly`/`edit-mode-done-btn`/`generateExcelFile`) | `AppState.selfPayManuallyOverridden` **플래그로만** "수동 수정 여부" 판단 — `lastCalculatedSelfPay > 0` truthy 체크 금지 | v1.6.215 | 전액 법인카드(itemSelfPay=0원) 항목이 구간표 참고값으로 잘못 새는 회귀 |
   | `_calcCorpForItem` | 법인카드 제안액 = `min(정책상 한계기여분, 클럽잔여예산 - 이번 세션 기배정 corporateAmount 실제합)` — 두 캡을 하나로 합쳐서 계산 금지 | v1.6.216 | 앞 항목 법인카드를 수동으로 예산 전액까지 올려도 뒤 항목에 계속 법인카드 제안됨 |
   | 비용 항목 추가 폼의 예산 초과 검증(① 클럽별, ② 전체 총 예산) | 검증 기준은 **"이 항목에 실제로 배정하려는 법인카드 금액"(`_requestedCorp`)** — 항목 전체 금액의 구간표 추정치(`_newSupport`)로 비교 금지 | v1.6.217 | 법인카드 0원(전액 자부담)으로 두어도 예산 소진 시 항목 추가 자체가 차단됨 |
   | `finalizeSettlement` | `clubRegistry.{id}.allowOverLimitSupport`(85,000원 초과 승인)는 **정산 확정 시 무조건 `false`로 리셋** — "사용했을 때만" 조건부 리셋 금지 | v1.6.221 | 1회성 승인이 영구 설정처럼 계속 켜진 채로 남음 |
   | 버전 bump 워크플로 | `sw.js`의 `APP_VERSION`과 `app.js` 최상단의 `APP_VERSION`/`APP_VERSION_DATE`를 **항상 함께** 올릴 것 — 하나만 올리고 끝내지 말 것 | v1.6.224 | "업데이트 내역" 모달(`CHANGELOG` 배열은 app.js 상수 기준)에서 신규 버전이 통째로 누락(v1.6.187~223, 37개 버전 사고) |
   | 대시보드 차트 클럽 색상 | 클럽 막대/라벨 색은 반드시 `getClubColorMap()`/`clubColorFor()`를 거쳐서 배정 — 차트 자체의 로컬 `palette[idx % ...]` (그 차트만의 정렬/필터 순서 기준 인덱스) 되돌리기 금지 | v1.6.226 | 같은 클럽인데 차트마다 색이 달라져 한눈에 매칭 안 되는 문제 재발 |
   | `_calcCorpForItem` 시설비 계산 | 시설·장비는 **항상 인당 `rules.facilityLimit`(기본 85,000원) × 참석 인원수** flat 한도만 적용 — 행사비 4구간 공식과 절대 풀링/합산하지 말 것(v1.6.228~229에 풀링 시도했다가 v1.6.232에서 "시설비는 인원수와 무관한 정액 비용"이라는 결론으로 최종 롤백됨). 한도는 정산 1건 전체 기준 — 같은 세션의 다른 시설비 항목 법인카드와 **합산**해 잔여분만 제안/허용하고, 수동 입력 초과는 제출 시 차단(⓪-2 검증) | v1.6.232~233 | 시설비가 인당 구간표에 잘못 얽히거나, 항목을 쪼개면 한도를 우회할 수 있게 됨 |
   | `clearClubData` / `resetSettlementSession` | `previousPrizeTotal`은 **0으로 하드리셋 금지** — 반드시 `clubRegistry[clubId].prizeUsed`로 복원 (`finalizeSettlement`와 동일 방식) | v1.6.233 | 클럽 전환/정산 초기화 직후 상품비 연 50만원 한도 검증이 "누적 0원" 기준으로 계산돼 한도 초과 등록이 통과됨 |
   | `finalizeSettlement` / `updateHistoryEntry` Firebase 쓰기 | 공유 이력·개인 이력·누적액은 **루트 다중 경로 update + 서버 증분**으로 원자 저장하고, 성공 전 세션 초기화 금지. 오류는 호출부까지 다시 throw | v1.6.239 | 저장 실패인데 성공 표시·입력 초기화 / 동시 정산 누적액 덮어쓰기 |
   | 이력 참석자 수정·삭제 / `buildDirectoryCountsForHistory` | 누적 횟수는 남은 현재 연도 이력 전체를 **사번 기준으로 재집계**하고 관리자·작성자 `directory`를 이력과 함께 원자 저장. 단순 `count - 1` 금지 | v1.6.240 | `counts[사번]`은 그대로라 화면 누적 횟수가 수정·삭제 전 값으로 남는 문제 |
   | 정산 이력 행사 사진 | 신규·수정 이력에 `eventPhotos`를 저장하고 `downloadHistoryExcel`에서 이력 사진으로 교체 후 기존 세션 사진을 `finally`에서 복원 | v1.6.240 | 과거 엑셀에 사진 누락 또는 현재 작성 중인 다른 정산 사진 혼입 |
   | 정산 이력 첨부 보관 | v1.6.242 이후 확정 이력의 사진·영수증 원본은 `globalHistory`에만 보관하고 개인 이력에는 `mediaStoredInGlobal` 표식과 메타데이터만 저장. 과거 이력은 보존 기한 전까지 원본 제거 금지 | v1.6.242 | 개인 이력 중복으로 Firebase 용량 급증 또는 과거 첨부 조기 유실 |
   | 현재 참석자 중복 차단 | `addAttendee`에서 이름과 무관하게 사번(`employeeId`) 중복을 검사하고, 수정 중인 본인만 예외로 둔다. 화면 입력 검사만으로 대체 금지 | v1.6.243 | 다른 호출 경로 또는 이름 오타로 동일 사번이 여러 명 등록되어 참석자 수·누적 횟수 왜곡 |
   | 참석자 중복 안내 UI | 동일 사번 차단 시 브라우저 `alert()`를 사용하지 말고 `#attendee-duplicate-hint`와 두 입력칸의 `aria-invalid`로 안내. 이름·사번 입력 또는 수정 취소 시 반드시 초기화 | v1.6.244 | 기본 브라우저 알림이 화면을 가리고 사용자 경험 저하 또는 이전 경고가 계속 남음 |
   | 관리자 이력 정렬 / 연간 예산 차트 | 이력은 `settlementDate` 최신순(`id`는 동률 보조), 연간 예산 사용액은 `priorUsed + 올해 finalSupportAmount`만 사용 | v1.6.241 | 늦게 등록한 과거 정산이 위로 노출 / 전년도 지출이 현재 연간 예산에 중복 합산 |

   - 위 항목 중 하나라도 되돌리거나 "리팩토링"하면 대응하는 버그가 즉시 재발함.
   - 구조 변경이 꼭 필요한 경우 먼저 사용자에게 위 표를 보여주고 명시적 승인을 받아야 함.

---

## 2. 작업 워크플로 (매 변경 시 필수)

```
1. 파일 수정 (app.js / index.html / style.css 등)
2. node --check app.js   ← 문법 오류 확인
3. 버전 상수 2곳을 함께 1 증가 (현재 1.6.241) — **아래 두 곳 모두 빠짐없이 bump**:
   - `sw.js` 의 `APP_VERSION`
   - `app.js` 최상단의 `APP_VERSION`/`APP_VERSION_DATE` (로그인 화면 버전 라벨의 즉시 표시값 +
     "업데이트 내역" 모달이 이 값을 기준으로 그룹핑함 — v1.6.187~223 구간에서 이 상수만 37개
     버전 동안 안 올라간 사고가 있었음, v1.6.224에서 재동기화)
4. `app.js`의 `CHANGELOG` 배열(변경이력 모달용, `initChangelog()` 근처)에 새 버전 항목 1줄 추가
5. git add -A && git commit -q -m "..." && git push -q
```

- `sw.js`는 네트워크 우선 캐시 전략 + 업데이트 배너 트리거용 — 변경 시 버전 bump 누락하면 사용자에게 변경사항이 반영 안 됨.
- `app.js`의 `APP_VERSION`/`APP_VERSION_DATE`는 로그인 화면 버전 라벨이 서버 재확인 전 "즉시" 표시하는
  값이자 `CHANGELOG` 모달의 유일한 데이터 소스 — `sw.js`만 올리고 이걸 빠뜨리면 인앱 "업데이트 내역"에
  새 버전이 누락된다.

---

## 3. 정산 계산 핵심 요약 (자세한 내용은 CALCULATION_SPEC.md 참조)

- 비용 카테고리: 행사비(EVENT) / 시설·장비(FACILITY) / 상품(PRIZE)
- 인당 행사비 = 행사비 합계 ÷ 참석자 수
- 인당 자부담 4단계 구간 (가/나/다/라, `AppState.rules`로 관리자가 한도·비율 조정 가능) — **정책상 최소
  참고값**으로만 쓰임 (인당 자부담 비용 표시, 엑셀 K20/K21/K22):
  - 가: ≤30,000원 → 0%
  - 나: ≤60,000원 → 20%
  - 다: ≤120,000원 → 60,000×20% + (초과분)×40%
  - 라: >120,000원 → 비용 - 85,000원
- **실제 자부담/지원금(v1.6.215+)** = 항목별 개인카드/법인카드 금액 합계(`itemSelfPay`/`itemSupportAmount`,
  카테고리 무관) — 최종 지원금·클럽 예산 차감·엑셀 K24/K25/L25/K30·이메일 보고서가 이 값을 기준으로 함.
  사용자가 "총 자부담 금액" 직접 수정 가능 (`lastCalculatedSelfPay` + `selfPayManuallyOverridden` 플래그),
  수정 시 그 값이 `finalSelfPay`로 우선 적용됨.
- 엑셀(`lib/template.xlsx`) 수식 캐시 문제로 K4~K30 등 핵심 셀은 앱이 직접 값 계산해 덮어씀.
- **행사비 인당 지원 한도 초과 확인(v1.6.221)**: (기존 법인카드 합계 + 이번 항목 법인카드) ÷ 인원이
  **정책 계산값**(구간표로 실제 행사비 계산한 인당 지원액)을 초과~85,000원 이내면 그 자리에서 이사진
  승인 팝업(`showCorpOverLimitModal`, 네=진행·아니요=정책값 원복). **85,000원 자체를 초과**하면 팝업 없이
  클럽별 사전승인 설정(`clubRegistry.{id}.allowOverLimitSupport`, 관리자가 "클럽 관리"에서 토글)만 확인 —
  승인된 클럽은 잔여예산 내 최대치로 자동 진행, 미승인이면 정책값으로 자동 원복. 두 경우 모두 클럽 잔여
  예산 자체는 초과 불가(하드캡). **이 승인은 1회성** — `finalizeSettlement()`에서 정산이 확정되면 실제
  사용 여부와 무관하게 자동으로 미승인 상태로 되돌아감(재사용 시 매번 재승인 필요). 시설·장비의 자체
  사전승인 흐름(`showFacilityApprovalModal`, v1.6.142)과는 별개.
- **시설·장비(FACILITY) 법인카드 제안 — 인당 flat 한도(v1.6.232, 최종)**: 시설비는 인원수에 비례하는
  "정액성 비용"이라 행사비 4구간 공식을 적용하지 않는다 — 대신 항상 인당 `rules.facilityLimit`(기본
  85,000원) × 참석 인원수까지만 법인카드로 제안하고, 초과분은 자동으로 자부담(개인카드)이 된다. 이
  계산은 세션의 다른 행사비/시설비 항목과 전혀 풀링·합산되지 않는다(v1.6.228~229에서 "행사비와 인당
  지원 한도 풀에 합산" 방식을 시도했었으나, "시설비는 인원수 무관 정액 비용"이라는 결론으로 v1.6.232에서
  최종 롤백됨). 카테고리 선택 시 "시설 및 장비 이용에 대해 임원진의 승인을 받으셨나요?" 안내 팝업
  (`showFacilityApprovalModal`)이 뜨지만 **확인 버튼 하나뿐이고 계산 분기가 없다** — 단순 알림일 뿐,
  위 공식은 팝업 응답과 무관하게 항상 동일하게 적용된다.

---

## 4. 권한 모델 요약 (자세한 내용은 PERMISSIONS.md 참조)

| 구분 | 비용 항목 CRUD | 정산 확정/이력 조회 | 이력 삭제 | 전사원 명부 등록 | 명부 삭제 | 명부 카운트 수정 | 정산 구간/비율 설정 | 클럽 관리 |
|---|---|---|---|---|---|---|---|---|
| 일반 회원 | O | O (본인 것만) | X | O (등록만) | X | X | X | X |
| 개발자(002531) | O | O (본인 것만) | X | O | O | X | X | X |
| 관리자(000000) | O | O (전체) | O | O | O | **O (전용)** | O | O |

- 전사원 명부 "올해 누적 N회" 카운트: **관리자만 직접 수정 가능** (v1.6.74), 정산 확정 시 자동 +1.
- 매년 1월 첫 로그인 시 직전 연도 카운트를 `directory[이름].history[연도]`에 보관 후 0으로 리셋 (삭제 아님).

---

## 5. 주요 UI/기능 구조 (최근 추가/변경된 핵심 영역)

- **차트 탭**: 월별 추이 / 클럽별 예산 소진율 / 카테고리 비중 / 클럽별 정산 횟수 / 행사별 참석 인원(v1.6.234, v1.6.236 가로 목록형, v1.6.245 최근 10건·왼쪽 레이블 항상 표시) /
  클럽별 참석 인원 중복 제외(v1.6.234) / 자부담 추이 — 전부 `maintainAspectRatio:false` +
  `.chart-canvas-wrap` (320px / 모바일 260px). 클럽 필터(`selectedOverallClubs`) 공통 적용.
- **차트 클럽 색상 통일(v1.6.226)**: 클럽 단위 막대를 그리는 모든 차트는 전부
  `getClubColorMap()`(전체 클럽 레지스트리를 이름순 고정 정렬해 만든 클럽명→색 매핑)을 통해 막대색을
  정함 — 같은 클럽은 항상 같은 색. 예산 소진율 차트는 막대색 대신 소진율 % 숫자 글씨색을
  `getUsageRiskColor()`(안전 초록/주의 노랑 80%↑/초과 빨강 100%↑)로 표시.
- **클럽별 참석 인원(중복 제외) 차트(v1.6.234)**: 고유 인원 판정은 사번(`employeeId`) 우선 — 동명이인은
  사번이 다르면 서로 다른 사람. 사번 없는 참석자만 이름 폴백. 올해(현재 연도) 이력만 집계, 많은 순 정렬.
- **클럽별 정산이력 탭** (관리자):
  - "클럽 선택" 드롭다운 (전체 클럽=월별 그룹, 특정 클럽=목록)
  - "정산인 선택" 드롭다운: 선택된 클럽 기준 정산인이 **2명 이상일 때만 표시**, 1명이면 자동 숨김 (v1.6.75)
  - 각 항목에 "정산인: OOO" 표시 (`entry.creatorName`)
  - 삭제는 관리자만 가능 — globalHistory + 작성자 개인 이력 + 참석자 누적 카운트 동시 처리
- **정산 결과 화면**: "클럽 잔여 예산" 표시 (연간 예산 - 사용액, 음수면 경고색)
- **정산 이력** (일반 회원): 조회만 가능, 삭제 불가 (관리자에게 요청)
- **관리자 시작 안내(v1.6.246)**: 대시보드 상단의 4단계 바로가기(클럽·예산 / 정산 기준 / 명부 / 현황)를 유지. 클럽·예산과 정산 기준은 클릭 시 접힌 해당 카드를 펼치고, 클럽 상태를 함께 표시.
- **PWA 업데이트 배너**: `reg.waiting` 직접 확인 후 500ms 내 강제 reload

---

## 6. 참고 문서 목록

- [`CALCULATION_SPEC.md`](CALCULATION_SPEC.md) — 계산식/엑셀 매핑 "변경 금지 기준" 문서 (최우선 참조)
- [`계산식_명세서.md`](계산식_명세서.md) — 계산식 + 명부 카운트 관리 설명 (한국어 상세본)
- [`PERMISSIONS.md`](PERMISSIONS.md) — 권한별 가능/불가 기능 정리
- [`CHANGES.md`](CHANGES.md) — 버전별 변경 이력
- [`CODE_REVIEW.md`](CODE_REVIEW.md) — **매 작업 시작 전 필독**: v1.6.201 검증 결과, 재수정 금지 항목 상세, 승인된 백로그(사용자 명시 요청 시에만 착수)
- [`ADMIN_DASHBOARD_UX_NOTES.md`](ADMIN_DASHBOARD_UX_NOTES.md) — 관리자 첫 화면 UX 진단과 적용한 개선 기록

현재 버전: **v1.6.246** (`sw.js` `APP_VERSION`)
