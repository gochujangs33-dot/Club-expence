# 관리자 첫 화면 UX 진단 및 개선 기록

## 최초 진단 (v1.6.245 이전)

신규 관리자가 첫 진입했을 때 다음 행동을 빠르게 정하기 어려운 원인을 기록한다.

1. 상단 메뉴는 기능 목록만 보여 주고, `클럽 등록 → 예산 설정 → 정산 기준 확인`의 진행 순서를 알려 주지 않았다.
2. 첫 화면의 누적 통계는 현황 파악에는 유용하지만, 처음 해야 할 행동을 알려 주지 못했다.
3. 핵심 설정인 `클럽 관리`와 `정산 구간 및 비율 설정`은 접힌 상태일 수 있어 처음 찾기 어려웠다.
4. `대시보드 관리`라는 메뉴명만으로는 통계 확인과 설정 중 무엇을 먼저 해야 하는지 알기 어려웠다.

## 적용한 개선 (v1.6.246)

- 관리자 대시보드 상단에 `무엇부터 할까요?` 시작 안내를 추가했다.
- 클럽 등록 전에는 `설정 필요`를, 등록 후에는 현재 클럽 수를 표시한다.
- 누적 통계 카드는 유지하되, 시작 안내보다 아래에 배치한다.

## 후속 조정 (v1.6.247)

- 전사원 명부 관리는 첫 화면의 핵심 시작 절차에서 제외했다.
- 시작 안내는 `클럽·예산 설정`, `정산 기준 확인`, `정산 현황 확인`의 3개 항목으로 간소화했다.
- `클럽·예산 설정`, `정산 기준 확인`은 같은 버튼을 다시 누르면 각각의 설정 카드를 접는 토글 방식으로 변경했다.
- 현재 열린 항목은 `접기` 문구와 강조색으로 구분한다.

## 확인 기준

- 신규 관리자가 메뉴를 탐색하지 않고도 클럽·예산 설정과 정산 기준 화면을 열 수 있어야 한다.
- 시작 안내의 설정 버튼과 카드 오른쪽 접기 버튼은 같은 열림·닫힘 상태를 유지해야 한다.
- 모바일에서도 시작 안내 항목이 화면 폭에 맞게 2열 또는 1열로 배치되어야 한다.
