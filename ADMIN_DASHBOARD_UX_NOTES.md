# 관리자 첫 화면 UX 진단 및 개선 기록

## 최초 진단 (v1.6.245 이전)

신규 관리자가 첫 진입 시 다음 이유로 업무 시작점을 한눈에 파악하기 어려웠다.

1. 상단 메뉴는 기능 목록만 보여 주고, `클럽 등록 → 예산 설정 → 정산 기준 확인 → 명부 관리` 순서가 보이지 않았다.
2. 첫 화면의 누적 통계는 현황 파악에는 유용하지만, 처음 해야 할 행동을 알려 주지 못했다.
3. 핵심 설정인 `클럽 관리`와 `정산 구간 및 비율 설정`이 접힌 상태일 수 있어 중요도를 알아차리기 어려웠다.
4. `대시보드 관리`라는 명칭만으로 통계 화면인지 설정 화면인지 판단하기 어려웠다.

## 적용한 개선 (v1.6.246)

- 관리자 대시보드 상단에 `무엇부터 할까요?` 시작 안내를 추가했다.
- 다음 4개 행동을 순서와 함께 바로 실행할 수 있게 했다.
  1. 클럽·예산 설정
  2. 정산 기준 확인
  3. 전사원 명부 관리
  4. 정산 현황 확인
- 클럽·예산 또는 정산 기준을 누르면 해당 카드가 자동으로 펼쳐지고 첫 입력칸으로 이동한다.
- 클럽 등록 전에는 `설정 필요`를, 등록 후에는 현재 클럽 수를 표시한다.
- 누적 통계 카드는 유지하되, 시작 안내보다 아래에 배치한다.

## 확인 기준

- 처음 접속한 관리자가 메뉴를 탐색하지 않고도 첫 설정 위치에 도달할 수 있다.
- 카드가 접혀 있어도 시작 안내 버튼으로 열 수 있다.
- 데스크톱에서는 4개 단계가 한 줄로, 좁은 화면에서는 2열 또는 1열로 읽힌다.
