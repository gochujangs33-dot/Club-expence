# 클럽 비용 정산기 — 항목별 계산 명세서

> 최종 업데이트: v1.6.141  
> **모든 데이터 기준 = 관리자(PIN: 000000) 설정값** — 유저 화면은 이를 읽기 전용으로 표시

---

## 0. 데이터 원칙 (변경 금지)

| 항목 | 저장 위치 (Firebase) | 유저 수정 가능 여부 |
|---|---|---|
| 자부담 구간/비율 | `globalSettings/rules` | ❌ 관리자 전용 |
| 클럽 연간 배정 예산 | `clubRegistry/{id}.budget` | ❌ 관리자 전용 |
| 이전 사용 금액 기준 | `clubRegistry/{id}.priorUsed` | ❌ 관리자 전용 |
| 상품비 연간 누적 | `clubRegistry/{id}.prizeUsed` | ❌ 관리자 전용 (정산 확정 시 자동 갱신) |
| 상품비 연간 한도 | `globalSettings/rules.prizeLimit` | ❌ 관리자 전용 |
| 전체 정산 이력 | `globalHistory/{id}` | ❌ 관리자만 삭제 가능 |
| 삭제 항목 마킹 | `deletedHistoryIds/{id}` | ❌ 시스템 자동 |
| 클럽 목록 | `clubRegistry/` | ❌ 관리자 전용 |
| 정산 구간 설정 | `globalSettings/rules` | ❌ 관리자 전용 |
| 세션 데이터 (진행 중) | `settlements/{PIN}` | ✅ 유저 (비용항목, 참석자 등) |

---

## 1. 비용 카테고리

| 코드 | 표시명 | 비고 |
|---|---|---|
| `EVENT` | 행사비 | 자부담 계산 기준 |
| `FACILITY` | 시설·장비 이용료 | 자부담 없음, 100만원 초과 시 경고 |
| `PRIZE` | 상품비 | 10명 이상·연 50만원 한도 제한 |

---

## 2. 자부담 계산식 (CALCULATION_LOCKED — 임의 수정 금지)

### 2-1. 인당 행사비

```
인당 행사비 = 행사비 합계 ÷ 참석자 수
```

### 2-2. 인당 자부담 (4구간)

관리자가 `globalSettings/rules`에서 한도·비율 조정 가능 (기본값 기준 설명)

| 구간 | 인당 행사비 범위 | 자부담 계산 | 예시 (인당 50,000원) |
|---|---|---|---|
| **가** | 0 ~ 30,000원 이하 | 0원 (전액 법인) | — |
| **나** | 30,001 ~ 60,000원 이하 | 인당행사비 × 20% | 50,000 × 20% = 10,000원 |
| **다** | 60,001 ~ 120,000원 이하 | 60,000×20% + (초과분)×40% | — |
| **라** | 120,001원 초과 | 인당행사비 − 85,000원 | — |

```
다구간 예시 (인당 100,000원):
  60,000 × 0.20 = 12,000
  (100,000 − 60,000) × 0.40 = 16,000
  인당 자부담 = 28,000원
```

### 2-3. 총 자부담 및 최종 지원금

```
총 자부담 = round(인당 자부담 × 참석자 수)
최종 지원금 = 총 비용 − 총 자부담
```

- 총 자부담은 정산 결과 화면에서 **관리자/유저 모두 직접 수정 가능** (lastCalculatedSelfPay → finalSelfPay)
- 수정된 값이 엑셀 K24/K25/K30, L25에 반영됨

---

## 3. 상품비 (PRIZE) 검증 및 팝업 목록

### 3-1. 카테고리 드롭다운에서 "상품" 선택 시

| 조건 | 팝업 문구 | 팝업 유형 | 동작 |
|---|---|---|---|
| 참석자 < 10명 | `10명 이상일 경우에만 사용 가능합니다.` | 🚫 block | 카테고리 자동으로 행사비로 복원 |
| 연 한도(기본 50만원) 전부 소진 | `상품비 연 한도 50만원을 모두 사용했습니다. 더 이상 상품비를 추가할 수 없습니다.` | 🚫 block | 카테고리 자동으로 행사비로 복원 |
| 잔여 한도 > 0, 이번이 첫 상품비 | `한 해에 총 500,000원의 상품비 사용이 가능합니다.` | 🎁 info | 확인 후 진행 |
| 잔여 한도 > 0, 이미 누적액 있음 | `올해 최대 {잔여금액}원까지 상품비 사용 가능합니다.` | 🎁 info | 확인 후 진행 |

### 3-2. "항목 추가" 버튼 클릭 시 (상품비 카테고리)

| 조건 | 팝업 문구 | 팝업 유형 | 동작 |
|---|---|---|---|
| 잔여 한도 ≤ 0 | `상품비 연 한도 50만원을 모두 사용했습니다. 더 이상 추가할 수 없습니다.` | 🚫 block | 추가 불가 |
| 입력금액 > 잔여 한도 | `상품비 연 한도 50만원을 초과할 수 없습니다. 최대 {잔여}원까지 입력 가능합니다.` | ⚠️ warn | 금액 자동으로 잔여 한도값으로 변경 |
| 클럽 잔여 예산 부족 | `클럽 잔여 예산이 부족합니다. 잔여 예산: {금액}원 상품비는 클럽 예산 내에서만 지출 가능합니다.` | 🚫 block | 추가 불가 |

### 3-3. 계산 시 경고 (정산 결과 화면)

| 조건 | 경고 문구 |
|---|---|
| 상품비 > 0 이고 참석자 < 10명 | `정회원 10명 이상 참석 시에만 상품비 사용이 가능합니다.` |
| 상품비 + 누적액 > 연 한도 | `상품비 연 한도 50만원을 초과할 수 없습니다.` |
| 시설비 > 1,000,000원 | `시설 및 장비 이용료가 1,000,000원을 초과하여 별도 협의가 필요합니다.` |

### 3-4. 잔여 한도 계산

```
잔여 한도 = prizeLimit − existingPrize(현재세션) − previousPrizeTotal(확정누적)

previousPrizeTotal = clubRegistry[clubId].prizeUsed  ← 관리자 기준 (단일 소스)
```

---

## 4. 클럽 잔여 예산

### 4-1. 계산

```
연간 배정 예산 = clubRegistry[clubId].budget        (관리자 설정)
사용 금액 = clubRegistry[clubId].priorUsed          (관리자 설정 기준 이전 사용분)
           + settlementHistory 이번 연도 finalSupportAmount 합계
잔여 예산 = 연간 배정 예산 − 사용 금액
```

- 잔여 예산이 음수이면 경고색(빨강) 표시
- 관리자 클럽관리 탭: 배정 예산 / 상품비 사용 / 잔여 예산 나란히 표시

### 4-2. 표시 위치

| 화면 | 표시 항목 |
|---|---|
| 정산 결과 (유저) | 클럽 잔여 예산 (현재 정산 지원금 차감 전) |
| 클럽 관리 (관리자) | 배정 예산 / 🎁 상품비 사용(한도) / 잔여 예산 |
| 정산 결과 (유저) | 이후 남은 잔여 금액 (이번 최종 지원금 차감 후) |

---

## 5. 엑셀 셀 매핑 (CALCULATION_LOCKED — 임의 변경 금지)

| 엑셀 셀 | 내용 |
|---|---|
| K4 | 참석자 수 |
| K6 | 행사비 합계 |
| K8 | 인당 행사비 |
| K10 | 시설·장비 이용료 |
| K12 | 상품비 |
| K14 | 총 소요 비용 |
| K16 | 인당 자부담 |
| K18 | 자부담 비율 |
| K20 | 총 자부담 (수정 가능값) |
| K22 | 최종 지원금 |
| K24 | finalSelfPay (수정된 자부담) |
| K25 | finalSelfPay 동일 |
| K30 | finalSelfPay 동일 |
| L12~L25 | 안내 문구 |

---

## 6. 정산 확정(finalizeSettlement) 흐름

```
1. 유저가 "엑셀 파일로 저장" 클릭
2. 정산 결과 계산 (SettlementCalculator.calculate)
3. SettlementValidator.validate 자동 실행 → 불일치 시 콘솔 에러
4. 엑셀 생성 (lib/template.xlsx 기반, 셀 직접 덮어쓰기)
5. 정산 이력 저장:
   - settlementHistory (유저 개인 이력)
   - globalHistory/{id} (관리자 전체 이력)
6. clubRegistry[clubId].prizeUsed += 이번 상품비
7. usedBudget += finalSupportAmount
8. 세션 초기화 (expenseItems, attendees, memberCount)
9. previousPrizeTotal = clubRegistry[clubId].prizeUsed (갱신 후 값)
```

---

## 7. 권한별 가능 기능 요약

| 기능 | 일반 유저 | 개발자(002531) | 관리자(000000) |
|---|---|---|---|
| 비용 항목 등록/정산 | ✅ | ✅ | ✅ |
| 본인 정산 이력 조회 | ✅ | ✅ | ✅ |
| 전체 정산 이력 조회 | ❌ | ❌ | ✅ |
| 정산 이력 삭제 | ❌ | ❌ | ✅ |
| 전사원 명부 등록 | ✅ | ✅ | ✅ |
| 명부 삭제 | ❌ | ✅ | ✅ |
| 명부 카운트 수정 | ❌ | ❌ | ✅ |
| 자부담 구간/비율 설정 | ❌ | ❌ | ✅ |
| 상품비 한도 설정 | ❌ | ❌ | ✅ |
| 클럽 등록/수정/삭제 | ❌ | ❌ | ✅ |
| 클럽 예산 배정 | ❌ | ❌ | ✅ |
| 클럽별 상품비 확인 | 본인 클럽만 | 본인 클럽만 | ✅ 전체 |

---

## 8. 실시간 동기화 구조

```
Firebase 실시간 리스너 (앱 실행 중 항상 최신 유지):
  globalSettings/rules  → AppState.rules (모든 유저)
  clubRegistry/         → AppState.clubRegistry (모든 유저)
  globalHistory/        → 관리자 정산이력 탭

로그인 시 1회 로드:
  settlements/{PIN}     → 세션 데이터 (expenseItems, attendees, clubName, clubId 등)
```

---

## 9. 앱 버전 관리

- `sw.js` `APP_VERSION` 변경 → 서비스워커 캐시 교체 → 업데이트 배너 표시
- 로그인 화면 "🔄 로그인 전에 이 버튼을 먼저 눌러주세요" 버튼: SW 해제 + 캐시 삭제 + 강제 리로드
- 현재 버전: **v1.6.141**
