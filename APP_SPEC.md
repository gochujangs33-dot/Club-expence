# 클럽 비용 정산기 — 전체 앱 명세서

> 버전: v1.6.141 | 최종 업데이트: 2026-06-25  
> **이 문서는 앱의 모든 계산 방법, 경우의 수, 예외 사항, 팝업 문구를 정리한 기준 문서입니다.**  
> **계산 관련 항목은 관리자의 명시적 서면 승인 없이 코드 수정 절대 금지.**

---

## 목차

1. [데이터 원칙](#1-데이터-원칙)
2. [비용 카테고리](#2-비용-카테고리)
3. [자부담 계산 (CALCULATION_LOCKED)](#3-자부담-계산)
4. [상품비 전체 로직](#4-상품비-전체-로직)
5. [카드 유형 (법인/개인/분할)](#5-카드-유형)
6. [클럽 잔여 예산 계산](#6-클럽-잔여-예산-계산)
7. [정산 결과 화면 항목](#7-정산-결과-화면-항목)
8. [자부담 수동 수정](#8-자부담-수동-수정)
9. [정산 확정 흐름](#9-정산-확정-흐름)
10. [이력 수정 모드](#10-이력-수정-모드)
11. [엑셀 파일 생성 & 셀 매핑](#11-엑셀-파일-생성--셀-매핑)
12. [전사원 명부 관리](#12-전사원-명부-관리)
13. [권한 모델](#13-권한-모델)
14. [모든 팝업/알림 목록](#14-모든-팝업알림-목록)
15. [차트 탭](#15-차트-탭)
16. [서비스워커 & 캐시 & PWA](#16-서비스워커--캐시--pwa)
17. [다국어 (i18n)](#17-다국어-i18n)

---

## 1. 데이터 원칙

**모든 기준은 관리자(PIN: 000000) 설정값이다. 유저 화면은 이를 읽기 전용으로 표시한다.**

| 데이터 항목 | Firebase 경로 | 수정 권한 |
|---|---|---|
| 자부담 구간·비율·상품비 한도 | `globalSettings/rules` | 관리자 전용 |
| 클럽 목록·배정 예산·이전 사용액 | `clubRegistry/{clubId}` | 관리자 전용 |
| 클럽별 상품비 누적 | `clubRegistry/{clubId}.prizeUsed` | 정산 확정 시 자동 갱신 |
| 전체 정산 이력 | `globalHistory/{id}` | 관리자만 삭제 |
| 삭제된 이력 마킹 | `deletedHistoryIds/{id}` | 시스템 자동 |
| 유저 세션 데이터 | `settlements/{PIN}` | 유저 본인 |

**유저 Firebase(`settlements/{PIN}`) 저장 항목 (세션 데이터만):**
- `memberCount`, `expenseItems`, `attendees`, `directory`, `clubName`, `clubId`, `reportEmail`, `settlementHistory`, `eventPhotos`

**첨부 저장 원칙 (v1.6.242+):**
- 행사 사진·영수증 원본은 확정 시 `globalHistory/{id}`에만 보관하고, `settlements/{PIN}/settlementHistory`에는 첨부를 제외한 메타데이터만 저장한다.
- 행사 사진은 긴 변 960px·목표 70KB, 영수증은 긴 변 1280px·목표 120KB 수준의 JPEG로 변환한다. 한 정산의 첨부 합계는 Base64 기준 6MB를 넘길 수 없다.
- 정산 연도 다음 해 4월 1일부터 해당 이력의 사진·영수증만 삭제한다. 금액, 참석자, 항목 등 정산 데이터는 삭제하지 않는다. 전체 이력 정리는 관리자 로그인 시 실행된다.
- `rules`, `annualBudget`, `usedBudget`, `previousPrizeTotal` → **저장·로드 금지** (관리자 경로 기준)

---

## 2. 비용 카테고리

| 코드 | 표시명 | 자부담 계산 | 제약 조건 |
|---|---|---|---|
| `EVENT` | 행사비 | ✅ 4구간 계산 기준 | 없음 |
| `FACILITY` | 시설·장비 이용료 | `calculate()`의 4구간 공식에는 미포함 — 법인카드 제안액은 항상 인당 85,000원(관리자 설정 가능) × 참석 인원수까지, 초과분은 개인카드(자부담)로 자동 제안. 수동 입력으로도 한도 초과 불가(제출 시 차단, 세션 내 시설비 항목 합산 기준 — v1.6.233) | 카테고리 선택 시 임원진 승인 안내 팝업(확인만, 계산 분기 없음) 표시 (v1.6.232) |
| `PRIZE` | 상품 | ❌ 없음 (전액 법인) | 10명 이상·연 한도 내 |

- 하나의 정산에 카테고리를 혼합 등록 가능 (행사비 + 시설비 + 상품비 동시 가능)
- 비용 항목은 최대 20건까지 엑셀에 반영 (sheet2 F5~F24)

---

## 3. 자부담 계산

> **⛔ CALCULATION_LOCKED — 이 섹션의 계산식은 관리자 명시적 서면 승인 없이 수정 불가**

### 3-1. 기본 파라미터 (DefaultRules — 관리자가 변경 가능)

| 파라미터 | 기본값 | 의미 |
|---|---|---|
| `limit1` | 30,000원 | 가구간 상한 (이하: 자부담 0%) |
| `limit2` | 60,000원 | 나구간 상한 |
| `rate2` | 0.2 (20%) | 나구간 자부담 비율 |
| `limit3` | 120,000원 | 다구간 상한 |
| `rate3` | 0.4 (40%) | 다구간 초과분 자부담 비율 |
| `deduction4` | 85,000원 | 라구간 공제액 |
| `prizeLimit` | 500,000원 | 상품비 연간 최대 한도 |

### 3-2. 계산 순서

```
인당 행사비 (F) = 행사비 합계 ÷ 참석자 수

인당 자부담 = calculateSelfPayPerPerson(F, rules)
총 자부담   = round(인당 자부담 × 참석자 수)
최종 지원금 = 총 비용 - 총 자부담
```

### 3-3. 인당 자부담 4구간

| 구간 | 조건 | 계산식 | 예시 (F=100,000원) |
|---|---|---|---|
| **가** | F ≤ 30,000원 | 0원 | — |
| **나** | 30,000 < F ≤ 60,000원 | F × 20% | F=50,000 → 10,000원 |
| **다** | 60,000 < F ≤ 120,000원 | 60,000×20% + (F−60,000)×40% | 12,000 + 16,000 = 28,000원 |
| **라** | F > 120,000원 | F − 85,000원 | F=150,000 → 65,000원 |

### 3-4. 계산 예시 전체

| 참석자 | 행사비 | 인당행사비 | 구간 | 인당자부담 | 총자부담 | 최종지원금 |
|---|---|---|---|---|---|---|
| 10명 | 200,000원 | 20,000원 | 가 | 0원 | 0원 | 200,000원 |
| 10명 | 500,000원 | 50,000원 | 나 | 10,000원 | 100,000원 | 400,000원 |
| 10명 | 900,000원 | 90,000원 | 다 | 24,000원 | 240,000원 | 660,000원 |
| 10명 | 1,500,000원 | 150,000원 | 라 | 65,000원 | 650,000원 | 850,000원 |

### 3-5. 교차 검증 (SettlementValidator)

`calculate()` 호출 시 `SettlementValidator.validate()` 자동 실행. 불일치 시:
- 콘솔에 `🚨 [정산 계산 검증 실패]` 오류 출력
- `result._validation.valid === false` 반환
- **커밋 전 반드시 원인 수정 필요** — 경고 무시 배포 금지

---

## 4. 상품비 전체 로직

### 4-1. 잔여 한도 계산

```
잔여 한도 = prizeLimit − 현재세션 상품비 합계 − previousPrizeTotal

previousPrizeTotal = clubRegistry[clubId].prizeUsed  (관리자 기준 단일 소스)
```

### 4-2. 카테고리 드롭다운에서 "상품" 선택 시 팝업

| 조건 | 팝업 문구 | 아이콘 | 카테고리 복원 여부 |
|---|---|---|---|
| 참석자 수 < 10명 | `10명 이상일 경우에만 사용 가능합니다.` | 🚫 | ✅ 자동으로 행사비 복원 |
| 잔여 한도 ≤ 0 | `상품비 연 한도 50만원을 모두 사용했습니다. 더 이상 상품비를 추가할 수 없습니다.` | 🚫 | ✅ 자동으로 행사비 복원 |
| 첫 상품비 (세션+누적 모두 0) | `한 해에 총 {prizeLimit}원의 상품비 사용이 가능합니다.` | 🎁 | ❌ (확인 후 선택 유지) |
| 이미 누적액 있음 | `올해 최대 {잔여}원까지 상품비 사용 가능합니다.` | 🎁 | ❌ (확인 후 선택 유지) |

### 4-3. "항목 추가" 버튼 클릭 시 (상품비 카테고리)

| 조건 | 팝업 문구 | 아이콘 | 동작 |
|---|---|---|---|
| 잔여 한도 ≤ 0 | `상품비 연 한도 50만원을 모두 사용했습니다. 더 이상 추가할 수 없습니다.` | 🚫 | 추가 불가 (return) |
| 입력금액 > 잔여 한도 | `상품비 연 한도 50만원을 초과할 수 없습니다. 최대 {잔여}원까지 입력 가능합니다.` | ⚠️ | 확인 시 금액 자동으로 잔여 한도값 입력 |
| 클럽 예산 잔여 < 상품비 | `클럽 잔여 예산이 부족합니다. 잔여 예산: {금액}원 상품비는 클럽 예산 내에서만 지출 가능합니다.` | 🚫 | 추가 불가 (return) |

### 4-4. 정산 결과 경고 (warnings 배열)

| 조건 | 경고 문구 |
|---|---|
| 상품비 > 0 이고 참석자 < 10명 | `정회원 10명 이상 참석 시에만 상품비 사용이 가능합니다.` |
| 상품비 + 누적액 > prizeLimit | `상품비 연 한도 50만원을 초과할 수 없습니다.` |

### 4-5. 연간 누적 추적 흐름

```
1. 클럽 선택 시: previousPrizeTotal ← clubRegistry[clubId].prizeUsed
2. 상품비 항목 추가 시: prizeInput 표시 = previousPrizeTotal + 현재세션 상품비 합계
3. 정산 확정 시: clubRegistry[clubId].prizeUsed += 이번 상품비
4. 확정 후: previousPrizeTotal = clubRegistry[clubId].prizeUsed (갱신된 값)
```

---

## 5. 카드 유형

### 5-1. 비용 항목당 카드 유형

| cardType | 법인카드 토글 | 개인카드 토글 | 저장 필드 |
|---|---|---|---|
| `corporate` (기본) | ON | OFF | `receiptImage` (법인 영수증) |
| `personal` | OFF | ON | `receiptImage` (개인 영수증) |
| `split` | ON | ON | `corporateReceiptImage` + `personalReceiptImage` |

### 5-2. 금액 입력 시 토글 자동 설정

금액 입력 또는 카테고리 변경 시 구간 계산 후 자동 설정:

| 구간별 법인카드 계산 결과 | 법인카드 토글 | 개인카드 토글 |
|---|---|---|
| 개인부담 0원 (전액 법인) | ✅ ON | ❌ OFF |
| 일부 자부담 발생 | ✅ ON | ✅ ON |
| 법인카드 0원 | ❌ OFF | ✅ ON |

- 법인카드 금액은 구간별 계산값으로 자동 입력 후 수동 수정 가능
- 개인카드 금액 = 총액 − 법인카드 (읽기 전용, 자동 계산)

### 5-3. 영수증 첨부

- 법인카드 영수증: `expense-receipt-corp-input`
- 개인카드 영수증: `expense-receipt-personal-input`
- 업로드 시 자동 압축 (`compressReceiptImage`)
- 영수증은 긴 변 1280px·목표 120KB 수준의 JPEG로 변환하며, 행사 사진을 포함한 한 정산의 첨부 합계는 Base64 기준 6MB 이하다.
- 상태 표시: "⌛ 영수증 압축 중..." → "✓ 영수증 대기 완료"
- 엑셀 sheet4에 삽입 (법인: B열, 개인: D열, 행 간격 16행)

---

## 6. 클럽 잔여 예산 계산

### 6-1. 계산식

```
연간 배정 예산 = clubRegistry[clubId].budget            (관리자 설정)
이전 사용 기준 = clubRegistry[clubId].priorUsed         (관리자 설정)
이번 연도 사용 = settlementHistory 중 올해 항목의 finalSupportAmount 합계
총 사용 금액   = 이전 사용 기준 + 이번 연도 사용
잔여 예산      = 연간 배정 예산 − 총 사용 금액
```

- 잔여 예산 < 0 이면 경고색(빨강) 표시
- `getClubBudget()` → `clubRegistry[clubId].budget` 기준 반환
- `getClubUsedBudget()` → priorUsed + 올해 settlementHistory 합산

### 6-2. 표시 위치

| 화면 | 표시 항목 |
|---|---|
| 비용 정산 탭 > 자동 정산 결과 | 클럽 잔여 예산 (이번 지원금 차감 전) |
| 비용 정산 탭 > 자동 정산 결과 | 이전 잔여 예산 / 이번 지원금(−) / 이후 남은 잔여 금액 |
| 관리자 > 클럽 관리 | 배정 예산 / 🎁 상품비 사용(한도) / 잔여 예산 |

### 6-3. 관리자 첫 화면 시작 안내

- 관리자 대시보드 상단에는 신규 관리자를 위한 `무엇부터 할까요?` 안내를 표시한다.
- 순서는 `클럽·예산 설정 → 정산 기준 확인 → 전사원 명부 관리 → 정산 현황 확인`이며, 첫 두 단계는 해당 접힌 카드를 자동으로 펼친다.
- 클럽이 없으면 `설정 필요`를 표시하고, 클럽이 있으면 현재 등록된 클럽 수를 표시한다.

---

## 7. 정산 결과 화면 항목

| 표시 항목 | 계산 출처 | 비고 |
|---|---|---|
| 최종 지원금 (지원 한도 내) | `totalCost − totalSelfPay` | 자부담 수정 전 자동값 |
| 총 자부담 금액 (수정 가능) | `totalSelfPay` → `lastCalculatedSelfPay` | 직접 수정 가능 |
| 인당 자부담 비용 | `perPersonSelfPay` | 수정 불가, 자동 계산 |
| 자부담 비율 | `totalSelfPay / totalCost` | |
| 총 소요 비용 | 모든 항목 합계 | |
| 행사비 합계 | EVENT 항목 합계 | |
| 시설 및 장비 합계 | FACILITY 항목 합계 | |
| 상품비 합계 | PRIZE 항목 합계 | |
| 인당 행사비 | 행사비 ÷ 참석자 수 | |
| 클럽 잔여 예산 | `getClubBudget() − getClubUsedBudget()` | |
| 이후 남은 잔여 금액 | 클럽 잔여 − 이번 최종 지원금 | |

---

## 8. 자부담 수동 수정

- 정산 결과 화면에서 "총 자부담 금액(수정 가능)" 필드 직접 입력 가능
- 수정값 → `AppState.lastCalculatedSelfPay`에 저장
- `finalSelfPay` 결정 규칙:
  ```
  finalSelfPay = lastCalculatedSelfPay > 0
      ? lastCalculatedSelfPay   // 수동 수정값 우선
      : totalSelfPay            // 자동 계산값
  ```
- `finalSelfPay`가 엑셀 K24/K25/K30, L25 안내 문구에 반영됨

---

## 9. 정산 확정 흐름

정산 확정은 "엑셀 파일로 저장" 버튼에서 시작됩니다.

```
1. confirm("정산을 확정하시겠습니까?") → 취소 시 중단
2. SettlementCalculator.calculate() 실행
3. SettlementValidator.validate() 자동 교차 검증
4. 엑셀 파일 생성 (lib/template.xlsx 기반)
5. Firebase 루트 다중 경로 update로 한 번에 원자 저장:
   - globalHistory/{id}                       [전체 이력 및 사진·영수증 원본]
   - settlements/{PIN}/settlementHistory      [유저 개인 이력, 첨부 원본 제외]
   - settlements/{PIN}/directory               [사번 기준 올해 누적 재집계]
   - clubRegistry/{clubId}.usedBudget/prizeUsed [서버 증분]
   - 개인 세션 비용·참석자·사진 초기화
6. 원격 저장 성공을 await — 실패 시 현재 입력을 유지하고 성공 처리 금지
7. 로컬 이력·명부·클럽 누적값 갱신
8. 세션 초기화: expenseItems=[], attendees=[], memberCount=0, lastCalculatedSelfPay=0, eventPhotos=[]
9. previousPrizeTotal = clubRegistry[clubId].prizeUsed (갱신 후 값)
10. 정산 이력 탭으로 자동 전환
```

**엑셀 저장 완료 alert 문구:**
```
📂 {날짜} 정산 엑셀 파일이 생성되어 다운로드 폴더에 저장되었습니다.
✅ 전체 항목이 초기화되었습니다.
📧 저장된 파일을 이메일로 보내주세요.
📋 이번 정산 내역은 [정산 이력] 탭에서 확인하실 수 있습니다.
```

---

## 10. 이력 수정 모드

- [정산 이력] 탭 또는 [클럽별 정산이력]에서 "✏️ 수정" 버튼 클릭
- 관리자 [클럽별 정산이력]은 실제 정산일(`settlementDate`) 최신순으로 표시하고, 같은 날짜는 저장 시각(`id`) 최신순으로 표시
- 수정 대상 항목을 정산 탭으로 복원 (비용항목, 참석자, 자부담값 등)
- 수정 모드 배너 표시 ("수정 중: YYYY.MM.DD")
- "엑셀 파일로 저장" 클릭 → 수정된 내용으로 엑셀 생성 + 이력 갱신
  - Firebase `globalHistory/{id}` 업데이트
  - Firebase `settlements/{PIN}/settlementHistory` 전체 덮어쓰기
  - 이력 항목에 `isEdited: true`, `editedAt` 추가
- "취소" 버튼 → 수정 모드 해제, 세션 데이터 원복

---

## 11. 엑셀 파일 생성 & 셀 매핑

> **⛔ CALCULATION_LOCKED — 셀 매핑 변경 시 관리자 승인 필수**

### 11-1. 생성 방식

- `lib/template.xlsx` 파일을 JSZip으로 열어 셀을 직접 덮어씀
- `calcChain.xml` 제거 + `fullCalcOnLoad="1"` 설정 → 열 때 전체 재계산
- 셀 덮어쓰기: `setCellValue(sheet2, 'CELL', value, isString)`

### 11-2. sheet2.xml 셀 매핑 (비용내역 시트)

| 셀 | 내용 | 값 |
|---|---|---|
| K4 | 참석자 수 | `calcResult.memberCount` |
| K6 | 총 비용 | `calcResult.totalCost` |
| K7 | 행사비 | `calcResult.eventCost` |
| K8 | 시설·장비 이용료 | `calcResult.facilityCost` |
| K9 | 상품비 | `calcResult.prizeCost` |
| K12 | 인당 행사비 (F) | `eventCost / memberCount` |
| L12 | 구간 안내 문구 | 아래 표 참조 |
| J15~J18 | 가/나/다/라 구간 라벨 | 현재 설정값 기준 |
| K15 | 가구간 인당 비용 | F ≤ limit1 ? F : 0 |
| K16 | 나구간 인당 비용 | limit1 < F ≤ limit2 ? F : (F > limit2 ? limit2 : 0) |
| K17 | 다구간 초과분 | limit2 < F ≤ limit3 ? (F−limit2) : (F > limit3 ? (limit3−limit2) : 0) |
| K18 | 라구간 인당 비용 | F > limit3 ? F : 0 |
| L15 | 가구간 자부담 | 0 |
| L16 | 나구간 자부담 | K16 × rate2 |
| L17 | 다구간 자부담 | K17 × rate3 |
| L18 | 라구간 자부담 | K18 > 0 ? K18 − deduction4 : 0 |
| K20 | 인당 최소 자부담 | L15+L16+L17+L18 |
| K21 | 총 최소 자부담 | K20 × memberCount |
| K22 | 총 최소 자부담 비율 | K21 / K6 |
| K24 | **실제 자부담** | `finalSelfPay` (수동 수정 우선) |
| K25 | 실제 자부담 비율 | K24 / K6 |
| L25 | 정산 결과 안내 | 아래 참조 |
| K30 | 총 회사 지원금 | K6 − K24 |
| C5~C124 | 참석자 사번 (최대 120명) | |
| D5~D124 | 참석자 이름 (최대 120명) | |
| F5~F24 | 비용 항목명 (최대 20건) | |
| G5~G24 | 비용 금액 | |
| H5~H24 | 카테고리 한글명 | |

### 11-3. L12 구간 안내 문구

| 조건 | 문구 |
|---|---|
| 행사비 = 0 | (빈 셀) |
| F ≤ limit1 | `전액지원` |
| F ≤ limit2 | `{rate2×100}% 자체 부담` |
| F ≤ limit3 | `'나' 구간 자부담 비용 + {limit2}원 초과 금액에 대해 {rate3×100}% 자체 부담` |
| F > limit3 | `{deduction4}원 이외 금액 자체 부담(최대 인당 {deduction4/10000}만원 지원)` |

### 11-4. L25 정산 결과 안내 문구

```
diff = finalSelfPay − k21(총최소자부담)

diff ≥ 0: "정산 문제 없음. 최소 자부담 비용보다 {diff}원 추가 부담함"
diff < 0: "최소 자부담 비용 미달. {-diff}원 추가 자부담 필요."
```

### 11-5. 사진 삽입 위치

| 사진 종류 | 시트 | 위치 | 크기 |
|---|---|---|---|
| 행사 사진(여러 장) | sheet3.xml | B3부터 5열 간격 | 장당 240×180px |
| 법인카드 영수증 | sheet4.xml | B5부터 16행 간격 | 220×300px |
| 개인카드 영수증 | sheet4.xml | D5부터 16행 간격 | 220×300px |

---

## 12. 전사원 명부 관리

### 12-1. 명부 구조

```js
directory[이름] = {
    id: "사번",       // 대표 사번
    ids: ["사번1", "사번2"],  // 동명이인 포함 전체 사번 배열
    count: N,         // 올해 누적 정산 참석 횟수
    history: { 2024: 3, 2025: 5 }  // 연도별 과거 카운트 보관
}
```

### 12-2. 자동 등록 (bulkImportDirectory)

- 앱 로그인 시 `lib/employee_directory.json` 로드 → 새 이름만 추가
- 기존 등록된 이름은 건드리지 않음
- 사번이 JSON에서 이름이 바뀐 경우 자동 이전 (예: "이진호(PS)" → "이진호")
- JSON에 없는 이름인데 모든 사번이 JSON에 있으면 자동 삭제

### 12-3. 누적 카운트 관리

- **정산 확정·이력 수정·삭제 시**: 남은 현재 연도 이력 전체를 사번 기준으로 재집계해 `count`와 `counts[사번]` 동기화
- **관리자 이력 수정·삭제 시**: 관리자 전체 명부와 정산 작성자의 개인 명부를 이력 변경과 함께 원자 저장
- **새해 첫 로그인 시**: 전년도 카운트를 `history[연도]`에 보관 후 count=0으로 리셋 (삭제 아님)
- **관리자 전용**: 화면에서 카운트 숫자 직접 수정 가능 (입력 필드 표시)
- 일반 유저: 카운트 숫자만 표시 (수정 불가)

### 12-4. 동명이인 처리

- 동일 이름에 사번이 2개 이상이면 팝업으로 사번 선택 (`showEmpIdPickerPopup`)
- 현재 참석자에는 이름과 무관하게 동일 사번을 두 번 등록할 수 없다. 추가·수정 중 중복이면 이름 입력란 옆에 빨간 `⚠ 이미 등록됨: {이름} (사번 {사번})` 문구를 표시하고 저장하지 않는다. 입력을 수정하면 문구는 즉시 사라진다.

### 12-5. 명부 삭제 권한

| 권한 | 삭제 가능 여부 |
|---|---|
| 일반 유저 | ❌ 불가 |
| 개발자 (002531) | ✅ 가능 |
| 관리자 (000000) | ✅ 가능 |

---

## 13. 권한 모델

| 기능 | 일반 유저 | 개발자(002531) | 관리자(000000) |
|---|---|---|---|
| 비용 항목 추가/수정/삭제 | ✅ | ✅ | ✅ |
| 참석자 추가/삭제 | ✅ | ✅ | ✅ |
| 전사원 명부 등록 | ✅ | ✅ | ✅ |
| 명부 삭제 | ❌ | ✅ | ✅ |
| 명부 카운트 직접 수정 | ❌ | ❌ | ✅ |
| 정산 확정 (엑셀 저장) | ✅ | ✅ | ✅ |
| 본인 정산 이력 조회 | ✅ | ✅ | ✅ |
| 전체 정산 이력 조회 | ❌ | ❌ | ✅ |
| 정산 이력 삭제 | ❌ | ❌ | ✅ |
| 이력 수정 | ✅ (본인만) | ✅ (본인만) | ✅ (전체) |
| 자부담 구간/비율 설정 | ❌ | ❌ | ✅ |
| 상품비 연 한도 설정 | ❌ | ❌ | ✅ |
| 클럽 등록/수정/삭제 | ❌ | ❌ | ✅ |
| 클럽 예산 배정/추가 배정 | ❌ | ❌ | ✅ |
| 클럽별 정산이력 탭 접근 | ❌ | ❌ | ✅ |
| 회원 관리 (수정/삭제) | ❌ | ❌ | ✅ |
| 요청사항 목록 확인 | ❌ | ❌ | ✅ |

---

## 14. 모든 팝업/알림 목록

### 14-1. 상품비 커스텀 모달 (showPrizeModal)

로그인 화면 하단 토스트 스타일, 다크 테마

| 발생 위치 | 조건 | 문구 | 아이콘 | 동작 |
|---|---|---|---|---|
| 카테고리 선택 | 참석자 < 10명 | `10명 이상일 경우에만 사용 가능합니다.` | 🚫 | 카테고리 행사비 복원 |
| 카테고리 선택 | 잔여 한도 ≤ 0 | `상품비 연 한도 50만원을 모두 사용했습니다. 더 이상 상품비를 추가할 수 없습니다.` | 🚫 | 카테고리 행사비 복원 |
| 카테고리 선택 | 첫 상품비 | `한 해에 총 {N}원의 상품비 사용이 가능합니다.` | 🎁 | 선택 유지 |
| 카테고리 선택 | 누적액 있음 | `올해 최대 {잔여}원까지 상품비 사용 가능합니다.` | 🎁 | 선택 유지 |
| 항목 추가 | 잔여 한도 ≤ 0 | `상품비 연 한도 50만원을 모두 사용했습니다. 더 이상 추가할 수 없습니다.` | 🚫 | 추가 불가 |
| 항목 추가 | 금액 > 잔여 | `상품비 연 한도 50만원을 초과할 수 없습니다. 최대 {잔여}원까지 입력 가능합니다.` | ⚠️ | 금액 자동 변경 |
| 항목 추가 | 클럽 예산 부족 | `클럽 잔여 예산이 부족합니다. 잔여 예산: {금액}원 상품비는 클럽 예산 내에서만 지출 가능합니다.` | 🚫 | 추가 불가 |

### 14-2. alert() (브라우저 네이티브)

| 발생 위치 | 조건/상황 | 문구 |
|---|---|---|
| 정산 확정 + 엑셀 저장 완료 | 저장 성공 | `📂 {날짜} 정산 엑셀 파일이 생성되어 다운로드 폴더에 저장되었습니다. ✅ 전체 항목이 초기화... 📋 ...` |
| 엑셀 다운로드 오류 | 생성 실패 | `엑셀 파일 생성 실패: {오류 메시지}` |
| 파일 다운로드 완료 | 공유용 파일들 저장 | *(i18n: alert.files_downloaded)* |
| 이력 수정 저장 실패 | Firebase 오류 | *(i18n: alert.save_error_network)* |
| 클럽명 미입력 | 새 클럽 등록 | *(i18n: alert.enter_club_name)* |
| 관리자 구간 저장 완료 | 저장 성공 | *(i18n: alert.rules_saved)* |
| 명부 삭제 제한 | 권한 없음 | *(i18n: alert.directory_delete_restricted)* |
| 이력 수정 제한 | 관리자만 가능 | *(i18n: alert.edit_restricted_admin_only)* |
| 엑셀 다운로드 실패 | 오류 | *(i18n: alert.excel_download_failed)* |
| 회원 등록 성공 | 등록 완료 | `{이름}` + *(i18n: alert.register_success)* |
| PIN 미입력/형식 오류 | 6자리 아님 | *(i18n: alert.pin_digit_required)* |
| PIN 중복 | 이미 사용 중 | *(i18n: alert.pin_already_used)* |
| 회원 수정 실패 | 오류 | *(i18n: alert.edit_user_failed)* |
| 회원 삭제 실패 | 오류 | *(i18n: alert.delete_user_failed)* |
| 총 예산 저장 실패 | Firebase 오류 | *(i18n: alert.total_budget_save_failed)* |
| 요청사항 선택 없이 삭제 | 선택 없음 | *(i18n: alert.select_items_to_delete)* |

### 14-3. confirm() (브라우저 네이티브)

| 발생 위치 | 문구 | 취소 시 동작 |
|---|---|---|
| 정산 확정 버튼 | *(i18n: confirm.finalize_settlement)* | 중단 |
| 참석자 전체 삭제 | *(i18n: confirm.clear_all_attendees)* | 중단 |
| 관리자 구간 기본값 복원 | *(i18n: confirm.reset_rules)* | 중단 |
| 로그아웃 | *(i18n: confirm.logout)* | 중단 |
| 관리자 정산 이력 삭제 | *(이름)의 정산 기록을 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.* | 중단 |
| 요청사항 삭제 | `선택한 {N}건의 요청사항을 삭제하시겠습니까?` | 중단 |
| 클럽 삭제 | `'{클럽명}' 클럽을 삭제하시겠습니까?` | 중단 |
| 회원 삭제 | `'{이름}' ({PIN}) 회원을 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.` | 중단 |
| 요청사항 개별 삭제 | `이 요청사항을 삭제하시겠습니까?` | 중단 |

### 14-4. 커스텀 모달 (HTML 오버레이)

| 모달 ID | 발생 시점 | 내용 |
|---|---|---|
| `reset-session-modal` | 정산 초기화 버튼 | 확인/취소 |
| `club-not-found-modal` | 선택한 클럽이 삭제됨 | "클럽을 찾을 수 없습니다" |
| `excel-saved-modal` | 엑셀 저장 완료 후 | 다음 단계 안내 |
| `pin-login-modal` | 로그인/회원가입 | PIN 키패드 |
| `receipt-modal` | 영수증 이미지 클릭 | 이미지 확대 라이트박스 |
| `email-report-modal` | 정산 보고 화면 | 엑셀 저장 / 공유 / 메일 |
| `prize-modal-overlay` | 상품비 조건 | 다크 테마 토스트 모달 |

### 14-5. 화면 내 오류 메시지 (form-error-msg)

| 위치 | 문구 |
|---|---|
| 명부 추가 폼 | `이미 동일한 이름과 EMP ID로 등록되어 있습니다: {이름} ({사번})` |
| PIN 로그인 | *(i18n: login.error)* |
| 회원 등록 | `가입 등록에 실패했습니다. 네트워크를 확인해 주세요.` |

---

## 15. 차트 탭

관리자 접속 시 차트 탭 활성화. Chart.js 기반.

| 차트 | ID | 데이터 소스 | 유형 |
|---|---|---|---|
| 월별 비용 추이 | `monthly-trend-chart` | globalHistory 월별 합산 | Line |
| 클럽별 예산 소진율 | `club-usage-chart` | clubRegistry 예산 vs `priorUsed` + 올해 globalHistory 지원금 | Bar (수평) |
| 카테고리별 누적 비용 비중 | `category-pie-chart` | globalHistory expenseItems 합산 | Doughnut |
| 자부담 추이 | `selfpay-trend-chart` | globalHistory 월별 자부담 합산 | Line |
| 클럽별 비용 지출 비교 | `club-spend-chart` | globalHistory expenseItems 클럽별 분류 | Bar 누적 수평 |

- 월별·소진율 차트: 다중 클럽 필터 지원 ("전체 선택" 체크박스 포함)
- 연간 예산 통계·소진율: 과거 연도 이력을 제외하고 `priorUsed + 올해 finalSupportAmount 합계`만 사용
- `selectedOverallClubs`, `selectedUsageClubs` → 선택된 클럽 Set
- 카테고리 파이 차트: `entry.expenseItems` 직접 읽어 카테고리별 합산 (구버전 `entry.eventCost` 필드 사용 금지)
- 행사별 참석 인원 차트: 실제 정산일 최신순 최근 10건을 가로 막대로 표시하며, 왼쪽 `클럽명 날짜` 레이블은 `autoSkip:false`로 모든 행사에 표시한다.

---

## 16. 서비스워커 & 캐시 & PWA

### 16-1. 캐시 전략

- 네트워크 우선(Network First) + 실패 시 캐시 폴백
- 성공 응답은 캐시에 최신으로 갱신 (`cache.put`)

### 16-2. 버전 업데이트 흐름

```
1. sw.js APP_VERSION 증가
2. install 이벤트: 새 캐시(club-expense-v{버전}) 생성
3. activate 이벤트: 이전 버전 캐시 삭제 + clients.claim()
4. 모든 탭에 SW_UPDATED 메시지 전송 → 업데이트 배너 표시
5. 사용자가 배너 클릭 → skipWaiting → 페이지 리로드
```

### 16-3. 캐시 초기화 버튼 (로그인 화면)

**위치**: PIN 키패드 하단  
**문구**: `🔄 로그인 전에 이 버튼을 먼저 눌러주세요`  
**동작**:
```
1. navigator.serviceWorker.getRegistrations() → 모든 SW 해제
2. caches.keys() → 모든 캐시 삭제
3. location.reload(true) → 강제 리로드
```

> ⚠️ 이 버튼은 앱 파일(JS/HTML/CSS) 캐시를 초기화합니다.  
> Firebase에 저장된 데이터는 영향 없습니다.

---

## 17. 다국어 (i18n)

- **지원 언어**: 한국어(ko), 영어(en)
- **파일**: `lib/i18n.js` (번역 키-값 객체)
- **적용 방식**:
  - HTML 정적: `data-i18n="키"` 속성 → `applyTranslations()` 호출 시 일괄 치환
  - JS 동적: `t('키')` 함수 호출
- **언어 전환**: 로그인 화면 pill 버튼 (ko/en)
- **적용 범위**: 헤더, 탭, 버튼, 경고 문구, 빈 상태 문구, badge 등 전체

---

## 부록: Firebase 데이터 구조

```
/globalSettings/rules            → 자부담 구간/비율/상품비한도 (관리자 저장)
/clubRegistry/{clubId}           → 클럽 정보 (name, budget, priorUsed, prizeUsed)
/clubTotalBudget                 → 전체 클럽 총 예산 합계
/globalHistory/{timestamp}       → 전체 정산 이력 및 사진·영수증 원본 (모든 유저)
/deletedHistoryIds/{timestamp}   → 삭제된 이력 ID tombstone
/settlements/{PIN}               → 유저별 세션 데이터 (expenseItems, attendees 등)
/settlements/{PIN}/settlementHistory → 유저 본인 정산 이력(신규 확정 이력은 첨부 원본 제외)
/requests/{key}                  → 유저 요청사항/피드백
```
